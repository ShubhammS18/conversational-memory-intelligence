# M1 — Persisted End-to-End Memory Slice

## G0 · 2026-08-29T12:41:53.0325398+05:30
- wiki pages read: `.genesis/wiki/index.md` entries for SQLite Memory Store, Admission and Indexing State Machine, User-Scoped Retrieval Boundary, Context Budgeting, and Startup Recovery and Index Reconciliation
- candidate design records read: `design/data_model.md`, `design/api_contracts.md`, ADR-005, ADR-007, and the D4 characterization artifacts named by PLAN M1
- implementation-notes searched for: "MemoryService", "RequestContext", "SQLite", "FAISS", "TokenCounter", and "M1" — none
- codebase grep: `MemoryService|RequestContext|AdmissionRequest|RetrievalRequest|TokenCounter` — zero Python implementation hits; `src/conversational_memory`, `tests`, and `pyproject.toml` do not exist
- decisions that bind us: `decisions-manifest`, `M1-implementation-bindings`, ADR-005, ADR-007
- document corrections: D4 prototype statements are preserved as historical evidence and qualified for the later integrated package before BUILD
- verdict: UNBUILT

## Pre-BUILD state
- active loop: none
- maker model: unbound; record the exact runtime-selected model when L1 activates
- next action: obtain or confirm the runtime maker-model identifier, activate L1 BUILD, and write the iteration-1 micro-plan before production code

## iter 1 · 2026-08-29T13:36:09.2698404+05:30
- gate G1: pass · skills considered: `genesis`, `agentic-swe-master`, `modular-architecture`, `production-readiness`; selected all four for workflow, routing, dependency boundaries, and packaging readiness
- scope: packaging and dependency-boundary first step only; SQLite, FAISS, embeddings, service logic, and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. add authoritative `pyproject.toml` with the approved runtime and `dev` dependency declarations;
  2. add architecture tests for package presence, dependency direction, dependency authority, and CLI help;
  3. add the minimal inert `src/conversational_memory/` layer packages and CLI entry point;
  4. install the `dev` extra and run diff, dependency, lint, type, architecture-test, and CLI-help checks.
- gate G2: pass · added one package manifest, seven minimal source files, and four passing architecture checks without adding memory-system behavior
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`; editable `dev` installation; `pip check`; Ruff; mypy; 4/4 architecture tests; and CLI help all exit zero
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- tool note: the compiled Windows mypy wheel was blocked by Application Control before analysis; reinstalling the same approved `mypy==1.20.2` release as a pure-Python wheel made the exact type-check command pass
- decision: keep the first step structurally inert and enforce approved dependencies with tests before adding application or infrastructure behavior
- next: await review of iteration 1 before extending M1 beyond the approved packaging first step

## iter 2 · 2026-08-30T18:15:09.9690062+05:30
- gate G1: pass · skills considered: `genesis`, `agentic-swe-master`, `modular-architecture`, `security-engineering`; selected all four for loop control, routing, inward-only domain dependencies, and credential-policy assurance
- scope: standard-library-only domain records and pure deterministic policies with unit tests; application, infrastructure, adapters, CLI behavior, and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. write unit tests for immutable records, canonical idempotency, credential rejection, ranking order, and complete-block budgets;
  2. implement the smallest pure domain modules that satisfy those tests;
  3. verify that domain imports remain standard-library-only and that existing architecture boundaries still pass;
  4. run diff, lint, strict type, architecture, and domain-unit checks.
- gate G2: pass · added five pure domain modules, updated the domain export surface, and added 46 domain tests across six test files
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, and 46 domain tests all pass (50 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: eligibility is an explicit upstream input in M1; this iteration filters it before ranking but does not implement the M2 lifecycle-eligibility policy
- next: await review of iteration 2 before extending M1 beyond the approved pure-domain scope

## iter 3 · 2026-08-31T12:12:43.2637410+05:30
- gate G1: pass · skills considered: `genesis`, `agentic-swe-master`, `modular-architecture`, `security-engineering`; selected all four for loop control, backend routing, application-owned dependency ports, and trusted identity and credential boundaries
- scope: synchronous application-layer admission contracts, ports, orchestration, and focused unit tests only; concrete infrastructure, retrieval, CLI behavior, and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. define frozen request, result, embedding, idempotency, and pending-persistence contracts without authoritative identity in the request body;
  2. define application-owned protocols for idempotency lookup, embedding, authoritative persistence, vector indexing, trusted time, and memory IDs;
  3. coordinate idempotency, credential admission, embedding, pending persistence, indexing, and final acknowledgement through those ports;
  4. prove ordering, trusted owner scope, zero-mutation replay/conflict/rejection paths, and partial indexing outcomes with test-only spies.
- gate G2: pass · added four application modules, expanded the application export surface, and added 20 focused application tests
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, and 20 application tests all pass (70 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: authoritative identity remains separate in `RequestContext`; every external effect is reached through an application-owned port, and idempotency or credential rejection stops before new persistence, embedding, or indexing mutations
- next: await review of iteration 3 before adding concrete M1 infrastructure adapters

## pre-iter 4 high-risk checkpoint · 2026-08-31T13:17:32.2067397+05:30
- baseline commit: `15b1b7f27eeb6f351d0738d157d28623737d6a1c`
- working tree: clean; `git status --short` produced no output
- baseline verification: `python -m pytest tests/architecture tests/unit/domain tests/unit/application -q` passed with 70 tests
- planned change: concrete SQLite persistence and durable FAISS generation handling
- risk: SQLite and FAISS cannot be committed atomically
- safety rule: SQLite remains authoritative; only `indexed` memories are retrievable
- failure rule: confirmed FAISS failure becomes `failed`; an unrecordable outcome remains `pending`; neither is retrievable
- recovery boundary: return to baseline commit `15b1b7f27eeb6f351d0738d157d28623737d6a1c` code and discard only iteration-4-generated test artifacts if BUILD verification fails
- scope: M1 only; no M2–M10 behavior

## iter 4A · 2026-08-31T13:42:25.4362740+05:30
- gate G1: pass · skills considered and selected: `genesis`, `agentic-swe-master`, `data-systems-engineering`, `modular-architecture`, `production-readiness` for loop control, transactional source-of-truth persistence, adapter boundaries, and restart and rollback assurance
- scope: concrete SQLite idempotency and memory-repository adapter, one numbered empty-database migration, and real-SQLite integration tests only; FAISS and all other M1 and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. bootstrap migration metadata and apply one numbered initial schema;
  2. atomically persist the pending memory, owner-scoped idempotency record, float32 embedding, and stable signed-int64 vector mapping;
  3. implement owner-scoped lookup and pending-to-indexed or pending-to-failed transitions;
  4. prove restart persistence, rollback, stable mapping, idempotency uniqueness, and cross-user isolation with temporary databases.
- gate G2: pass · added the SQLite adapter, migration runner, one numbered migration, infrastructure exports, and 12 infrastructure integration tests
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, 20 application tests, and 12 infrastructure integration tests all pass (82 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: one `BEGIN IMMEDIATE` transaction owns all four pending-persistence inserts; owner/key conflicts roll back every insert, and only an owner-scoped pending row may transition to `indexed` or `failed`
- next: await review of iteration 4A before implementing durable FAISS generation handling

## iter 4B · 2026-08-31T15:02:59.1143157+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, `data-systems-engineering`, and `production-readiness` for loop control, backend routing, derived-index consistency, and fail-closed persistence boundaries
- scope: concrete durable FAISS `VectorIndexPort` adapter and temporary-directory integration tests only; SQLite/FAISS service integration, retrieval, reconciliation, rebuilding, SentenceTransformers, CLI behavior, and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. validate configured model, dimension, float32 values, and stable signed-int64 vector IDs;
  2. clone `IndexIDMap2(IndexFlatIP)`, replace one stable ID, and file-sync verified temporary generation files;
  3. replace the fixed final index before its canonical metadata and load only a fully verified pair;
  4. prove restart persistence, duplicate prevention, fail-closed startup, every replacement boundary, and stale-temporary handling.
- gate G2: pass · added the FAISS adapter, two approved application error types, infrastructure export, and 34 focused FAISS integration tests
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, 20 application tests, and 46 infrastructure integration tests all pass (116 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: startup requires the fixed final pair unless empty creation is explicitly authorized; writes verify temporary and final pairs, replace the FAISS file before metadata, retain the prior in-memory index on failure, and never select temporary files
- next: await review of iteration 4B before integrating the SQLite and FAISS adapters

## iter 4C · 2026-08-31T19:09:37.9238290+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, `modular-architecture`, `data-systems-engineering`, and `production-readiness` for loop control, Backend/API routing, outward composition, and cross-store retry correctness
- scope: compose the existing `MemoryService`, `SQLiteMemoryRepository`, and `FaissVectorIndex` with test-only deterministic providers and six real end-to-end integration tests; retrieval, rebuilding, reconciliation, SentenceTransformers, CLI behavior, and M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. expose stored embedding and stable vector mapping as application-owned retry work;
  2. retry only existing `pending` or `failed` admissions without recomputing identity or embeddings;
  3. wire concrete SQLite and FAISS adapters in the composition layer while keeping application imports inward-only;
  4. prove success, state eligibility, both partial-failure paths, retry deduplication, idempotency, conflicts, and credential rejection with temporary storage.
- gate G2: pass · added the minimal composition function, stored-work retry contract and port transition, SQLite stored-work mapping, and six real SQLite–FAISS service integration tests
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, 20 application tests, and 52 integration tests all pass (122 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: indexed replay returns the stored result without mutation; pending and failed replay reuses SQLite’s stored float32 embedding and stable vector ID, and only a successful SQLite acknowledgement makes the result retrievable
- next: await review of iteration 4C before extending M1

## iter 5 · 2026-09-01T13:07:01.8559646+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, `modular-architecture`, `security-engineering`, and `data-systems-engineering` for loop control, Backend/API routing, inward-only ports, owner authorization, and SQLite-authoritative derived-index filtering
- scope: M1 owner-scoped retrieval contracts, ports, SQLite indexed-ID authorization and hydration, FAISS allowlist search, and 17 temporary-storage integration tests; M2 lifecycle behavior, context budgeting, CLI behavior, recovery, and all other M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. validate the trusted context, normalized non-empty query, and positive integer result limit;
  2. resolve the owner-scoped `indexed` vector-ID allowlist from authoritative SQLite and short-circuit an empty set before embedding or FAISS;
  3. restrict FAISS search to that allowlist, hydrate through owner-scoped SQLite, and reject any unexpected or no-longer-authorized result;
  4. prove isolation, pending/failed exclusion, empty short-circuiting, unexpected-ID rejection, and request validation using temporary SQLite and FAISS storage.
- gate G2: pass · added application-owned retrieval records and port methods, synchronous service orchestration, concrete scoped SQLite/FAISS operations, and 17 focused integration tests without reversing the dependency direction
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, 20 application tests, and 69 integration tests all pass (139 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: SQLite supplies the complete trusted-owner `indexed` allowlist before query embedding and FAISS search; empty allowlists perform neither operation, and FAISS or hydration results outside that scope fail closed
- next: await review of iteration 5 before extending M1

## iter 6 · 2026-09-01T13:40:56.3525869+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, and `modular-architecture` for loop control, fixed context-budget policy, application-owned tokenization boundary, and outward infrastructure composition
- scope: M1 retrieval ranking and complete-memory context selection using the approved serialization plus a real infrastructure `cl100k_base` token-counter adapter and 15 focused integration cases; truncation, summarization, lifecycle expansion, response generation, and all M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. add a memory-only token budget and exact context evidence to the application retrieval contracts;
  2. expose token counting through an application-owned port and implement fixed `cl100k_base` behind infrastructure;
  3. rank authorized hydrated memories with the existing domain policy, then run existing whole-context selection without additive token assumptions;
  4. prove exact fit, overflow, skip-and-continue, empty and zero budgets, invalid budgets, Unicode, and separator accounting with real tokenization.
- gate G2: pass · integrated the existing domain ranking and context-selection policies into `MemoryService`, added one token-counter port and adapter, counted special-token-looking content as ordinary text, translated tokenizer initialization failures without fallback, and added real-tokenizer evidence without introducing an inward dependency on `tiktoken`
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, four architecture tests, 46 domain tests, 20 application tests, and 84 integration tests all pass (154 passed total)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: the caller-provided budget is memory-only; every prospective context is serialized and counted as a whole with `cl100k_base`, special-token-looking text is encoded as ordinary text, tokenizer initialization fails with `ValidationError` and no fallback, oversized blocks are excluded without truncation, and later smaller ranked candidates remain eligible to fit
- next: await review of iteration 6 before extending M1

## iter 7 · 2026-09-01T14:25:21.2078700+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, and `llmops-ai-agents` for loop control, Backend/API and Memory Architecture routing, immutable model identity, cache-only loading, and real embedding evidence
- scope: M1 infrastructure `EmbeddingPort` adapter for the approved immutable `sentence-transformers/all-mpnet-base-v2` revision, CPU-only cache loading, eleven adapter cases, and one explicitly selected real-model restart/isolation/context integration test; dependency changes, downloads, silent store migration, CLI behavior, M8 recovery, and all M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. bind the canonical load name, immutable revision, revision-qualified persisted identity, CPU device, configured cache-only behavior, normalized float32 output, and typed failures;
  2. implement the existing application-owned embedding port behind infrastructure without changing `MemoryService`;
  3. prove load arguments, output validation, identity, dimension mismatch, missing/incomplete cache, load failure, and encode failure using adapter tests;
  4. register and run a separate required real-model test using temporary SQLite and FAISS storage, restart, owner isolation, and exact bounded context.
- gate G2: pass · added one infrastructure adapter and export, one registered test marker, eleven adapter tests covering modern and legacy dimension APIs plus selected-accessor failure, and one real-model integration test while preserving the application-owned port and keeping model loading outside `MemoryService`
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: pass · `git diff --check`, Ruff, mypy, and 165 routine tests pass with the real-model marker explicitly deselected; the required cache-only real-model test separately passes (166 total collected tests)
- gate G5: deferred · independent L4 verification runs only when the complete M1 milestone is ready to exit
- decision: new real-model stores persist `sentence-transformers/all-mpnet-base-v2@e8c3b32edf5434bc2275fc9bab85f82640a19130`; the adapter loads only that cached revision on CPU, selects callable `get_embedding_dimension` when available and otherwise callable `get_sentence_embedding_dimension` without retrying after a selected-accessor failure, emits finite normalized 768-dimensional float32 values, and never downloads, falls back, migrates, or relabels an existing store
- next: await review of iteration 7 before extending M1

## iter 8 · 2026-09-01T19:34:31.1159092+05:30
- gate G1: pass · selected `genesis`, `agentic-swe-master`, `modular-architecture`, and `production-readiness` for loop control, locked Backend/API closure, inward dependency enforcement, and integration-failure evidence
- scope: audited M1 closure gaps only—Pydantic public-boundary validation, the exact `demo-first-slice` CLI, locked verification paths consolidated over existing scenarios, deterministic restart evidence, and one real-component FAISS persistence-failure proof; dependencies, downloads, optional documents, and all M2–M10 behavior remain excluded
- maker model: `gpt-5.6-sol`
- micro-plan:
  1. use strict frozen Pydantic dataclasses for public request/result boundaries while preserving standard-library frozen domain records and trusted `RequestContext` identity;
  2. compose the real local adapters only in `composition` and expose the exact restart/isolation/context CLI demonstration through the entry point;
  3. move context-budget regression evidence to its locked path and collect existing SQLite–FAISS scenarios through the exact admission, idempotency, and partial-failure paths without copying scenario implementations;
  4. add deterministic restart coverage and a cache-only real-model test that induces an ordinary real FAISS persistence failure by making its configured generation directory unavailable.
- gate G2: pass · added Pydantic validation to six public boundary records, one concrete local composition function, one exact CLI subcommand, five locked acceptance paths, one moved regression file, and two new acceptance scenarios; existing domain logic, dependency versions, and M2–M10 scope remain unchanged
- gate G3: pass · iteration remained within the 50000-token milestone cap; exact token metering is not exposed by this runtime
- gate G4: blocked · `git diff --check`, Ruff, mypy, editable installation with the already-installed build backend, `pip check`, CLI help, 75 architecture/unit tests, and the complete graph check (valid JSON, valid endpoints, acyclic, three invariants) pass; the exact isolated setup first attempted a prohibited PyPI lookup, and every FAISS-backed verification/demo command then failed during import because Windows Application Control blocked `.venv/Lib/site-packages/faiss/_swigfaiss.pyd`; no download, dependency replacement, or policy bypass was performed
- gate G5: deferred · independent L4 verification cannot start until the exact BUILD verification and demo commands pass
- decision: preserve the approved `faiss-cpu==1.15.0` environment and fail the BUILD gate honestly rather than replacing the dependency, weakening tests, or bypassing workstation execution policy
- next: restore execution approval for the unchanged installed FAISS DLL, rerun every locked quality/demo command, and update this same iteration with verified counts before requesting L4

## iter 8 validator correction · 2026-09-02T09:27:48+00:00
- gate G1: pass · used `agentic-swe-master` with the existing M1 Backend/API route and read the relevant Dependency Rule and Ruthless Testing references from the task-local Agentic SWE wiki root
- gate G2: pass · added one `RetrievalRequest.query` field validator that rejects empty and whitespace-only strings while returning valid text unchanged and preserving strict string validation
- gate G3: pass · correction remained within the existing M1 budget; exact token metering is not exposed by this runtime
- gate G4: routine pass · Ruff passes; mypy reports no issues in 22 source files; routine pytest reports 171 passed and 2 real-model tests deselected; `git diff --check` passes
- gate G5: deferred · required cache-only real-model verification and independent review remain pending, so M1 stays open
- decision: mirror `RequestContext`'s existing Pydantic validator pattern at the retrieval boundary without changing normalization, dependencies, tests, or behavior outside the reported construction-time validation gap
- next: run the pending cache-only real-model verification, then request independent L4 review without declaring M1 complete beforehand

## iter 8 Linux real-model verification · 2026-09-02T09:36:49+00:00
- cache evidence: copied only the 11 regular files in Windows snapshot `/mnt/c/Users/Asus/.cache/huggingface/hub/models--sentence-transformers--all-mpnet-base-v2/snapshots/e8c3b32edf5434bc2275fc9bab85f82640a19130` into the new isolated Linux cache `/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub/models--sentence-transformers--all-mpnet-base-v2/snapshots/e8c3b32edf5434bc2275fc9bab85f82640a19130`; the source snapshot contains no symlinks or referenced blobs
- cache verification: recursive `diff -qr` exits zero; all 11 per-file SHA-256 digests match; both snapshots contain 11 files and report 438682922 bytes
- verification environment for model processes: `CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub`, `HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, and empty `CUDA_VISIBLE_DEVICES`; the adapter also passes `device="cpu"`, `local_files_only=True`, and immutable revision `e8c3b32edf5434bc2275fc9bab85f82640a19130`
- real-model command: `.venv/bin/python -m pytest tests/integration/test_real_model_first_slice.py::test_real_model_restart_owner_scope_and_bounded_context tests/integration/test_first_slice.py::test_real_faiss_persistence_failure_is_failed_and_nonretrievable -q -m real_model` · 2 passed in 14.05s; neither test skipped
- demo command: `.venv/bin/python -m conversational_memory.entrypoints.cli demo-first-slice` · exit zero using its internal temporary SQLite/FAISS store; admission accepted/indexed/retrievable, replay returned the same memory ID and result, restart retrieval selected that ID with `cl100k_base` using 39 of 128 tokens, and the other user received zero memories and empty context
- packaging smoke: `.venv/bin/python -m conversational_memory.entrypoints.cli --help` · exit zero and lists only `demo-first-slice`
- locked M1 command: `.venv/bin/python -m pytest tests/architecture tests/unit tests/adapter tests/integration/test_admission_indexing.py tests/integration/test_first_slice.py tests/integration/test_idempotent_admission.py tests/integration/test_partial_index_failure.py tests/integration/test_restart_persistence.py tests/regression/test_context_budget.py -q` · 103 passed in 10.32s
- complete-suite command: `.venv/bin/python -m pytest tests -q` · 173 passed in 12.69s, including both real-model tests
- remaining quality evidence: `.venv/bin/python -m ruff check src tests` passes; `.venv/bin/python -m mypy src/conversational_memory` reports no issues in 22 source files; `.venv/bin/python -m pip check` reports no broken requirements; `git diff --check` exits zero with pre-existing CRLF normalization warnings
- installed versions: Python 3.12.3; conversational-memory-intelligence 0.1.0; pydantic 2.13.5; faiss-cpu 1.15.0; sentence-transformers 5.7.0; transformers 5.16.1; huggingface-hub 1.29.0; torch 2.14.0+cpu; numpy 2.5.2; tiktoken 0.14.0; pytest 9.1.1; Ruff 0.16.5; mypy 1.20.2
- gate G4: pass · locked Linux verification, real-model evidence, temporary-store demo, complete suite, lint, type checking, dependency consistency, packaging smoke, and diff check all pass
- gate G5: pending · independent L4 review has not run; after an APPROVE verdict, the required human quiz-me Q&A must be completed and logged before M1 can close
- decision: keep M1 open and preserve the verified isolated cache and all implementation changes unchanged pending independent review and human checks
- next: run independent L4 verification with the locked M1 spec, diff, command evidence, and context-graph invariants

## iter 8 L4 rejection and ranking correction · 2026-09-02T10:18:13+00:00
- gate G5: reject · an independent fresh-context reviewer found that `rank_candidates` ordered equal-relevance candidates by authority before recency, contradicting locked `DONE.html` language requiring newer memory before explicit-user authority; the existing ranking test encoded the same incorrect hierarchy
- accepted clarification: for M1 retrieval ordering, relevance is descending and “similar-score” means exact equality only; exact ties use recency descending, then explicit-user authority, then the existing stable-ID order; no epsilon, rounding, quantization, or score bucket is permitted, and this interpretation overrides ADR-004 only for M1 ordering of already eligible candidates
- binding evidence: added the approved scoped interpretation and ADR-004 distinction to `.genesis/decisions/M1-implementation-bindings.md`; locked `DONE.html`, `PLAN.md`, ADR-004 history, D4 experiments, lifecycle, supersession, and contradiction resolution remain unchanged
- regression-before evidence: with the new tests and original authority-before-recency implementation, `.venv/bin/python -m pytest tests/unit/domain/test_ranking.py -q` exited 1 with 2 failed and 5 passed; failures were `test_equal_relevance_prefers_recency_before_authority` and `test_input_order_does_not_change_ranking`
- correction: moved the existing recency key ahead of the existing authority key while retaining relevance first and stable ID last; added focused cases for newer inferred versus older explicit at equal relevance, authority at equal relevance and recency, stable ID at complete tie, one-ULP unequal relevance via `math.nextafter`, and input-order independence
- regression-after evidence: `.venv/bin/python -m pytest tests/unit/domain/test_ranking.py -q` · 7 passed in 0.05s
- verification environment for model processes: `CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub`, `HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, and empty `CUDA_VISIBLE_DEVICES`
- quality evidence: `.venv/bin/python -m ruff check src tests` passes; `.venv/bin/python -m mypy src/conversational_memory` reports no issues in 22 source files
- locked M1 command: `.venv/bin/python -m pytest tests/architecture tests/unit tests/adapter tests/integration/test_admission_indexing.py tests/integration/test_first_slice.py tests/integration/test_idempotent_admission.py tests/integration/test_partial_index_failure.py tests/integration/test_restart_persistence.py tests/regression/test_context_budget.py -q` · 107 passed in 15.30s
- real-model command: `.venv/bin/python -m pytest tests/integration/test_real_model_first_slice.py::test_real_model_restart_owner_scope_and_bounded_context tests/integration/test_first_slice.py::test_real_faiss_persistence_failure_is_failed_and_nonretrievable -q -m real_model` · 2 passed in 9.64s; neither test skipped
- demo command: `.venv/bin/python -m conversational_memory.entrypoints.cli demo-first-slice` · exit zero using its temporary store; admission indexed/retrievable, replay identical, restart retrieval selected the admitted memory with 34 of 128 `cl100k_base` tokens, and the other owner received zero memories and empty context
- complete-suite command: `.venv/bin/python -m pytest tests -q` · 177 passed in 12.44s, including both real-model tests
- diff evidence: `git diff --check` exits zero with pre-existing CRLF normalization warnings
- preserved review limitations: the repository-requested verify/requesting-code-review skill was unavailable to the rejecting reviewer; exact isolated `pip install -e .` remained unverified because build isolation attempted unavailable PyPI while the reviewer’s offline `--no-build-isolation` equivalent passed; prior model-cache provenance was not independently reconstructed
- gate G4: pass · the bounded ranking correction and all requested verification commands pass
- gate G5: pending re-review · M1 remains open; a fresh independent L4 verdict is required, followed by the mandatory human quiz-me Q&A only after APPROVE
- decision: ranking order affects only deterministic ordering of already eligible M1 retrieval candidates and neither establishes truth nor authorizes any state change
- next: submit the corrected binding, implementation, tests, and verification evidence to a fresh independent L4 reviewer

## iter 8 L4 concurrency rejection and bounded correction · 2026-09-02T12:55:00+00:00
- gate G5: reject · an independent fresh-context reviewer demonstrated that concurrent identical admissions could both pass the unlocked idempotency lookup, after which one returned `StorageError("SQLite pending-memory transaction failed")`; the adapter-local SQLite and FAISS locks did not cover the complete process-scoped workflow
- binding scope: the existing requirement that “Admissions and retries use the approved process write lock” applies to concurrent calls through one service and multiple service/adapter instances in one Python process; no cross-process or distributed lock was added
- correction: added one application-level process lock covering idempotency lookup/resolution, policy, embedding, pending persistence, FAISS publication, and SQLite acknowledgement for both new admissions and retries; exception exits release the lock, and retry does not reacquire it
- instance coherence: before each serialized add, `FaissVectorIndex` now reloads and verifies the fixed durable finals and clones that current generation, preventing a second supported in-process adapter from replacing a newer generation with its stale startup copy
- regression-before evidence: with the new concurrency regressions and the original implementation, the first focused run produced 3 failures and 1 pass: identical concurrency returned an exception and conflicting concurrency returned incidental `StorageError`; the third failure was an initial stale-instance fixture defect, corrected by sharing its deterministic ID source. Two read-only behavior toggles against the corrected tree then reproduced the remaining original defects exactly: replacing the workflow lock with `nullcontext()` made `test_retry_and_new_admission_share_process_write_serialization` fail `max_active == 1`, and restoring stale in-memory cloning made `test_two_service_instances_preserve_each_others_published_vectors` fail the expected two-vector count
- regression-after evidence: `.venv/bin/python -m pytest tests/integration/test_idempotent_admission.py -q -k 'concurrent or releases or two_service or retry_and_new'` · 5 passed and 2 deselected in 11.32s; cases prove one embedding/publication for identical calls, defined conflict behavior, shared admission/retry serialization, lock release after failure, and preservation of both vectors across two service/adapter instances
- quality evidence: `.venv/bin/python -m ruff check src tests` passes; `.venv/bin/python -m mypy src/conversational_memory` reports no issues in 22 source files
- verification blocker: broader focused and locked tests initialize `TiktokenTokenCounter`, but no local `cl100k_base` data was found in the available Linux cache or the checked task-relevant Windows cache locations; initialization attempted `openaipublic.blob.core.windows.net/encodings/cl100k_base.tiktoken` and failed name resolution. No download, dependency change, fallback, or test weakening was attempted, so architecture, locked M1, real-model, demo, and full-suite verification remain to be rerun after the approved cache prerequisite is restored
- remaining non-network evidence: `.venv/bin/python -m pip check` reports no broken requirements; `git diff --check` exits zero with pre-existing CRLF normalization warnings
- preserved review limitations: the repository-requested verify/requesting-code-review skill remains unavailable; exact isolated editable installation remains unverified because build isolation requires unavailable PyPI; prior model-cache provenance remains not independently reconstructed
- gate G4: blocked · focused concurrency behavior, Ruff, and explicit-source mypy pass, but required complete verification cannot currently execute from the available offline caches
- gate G5: pending re-review · M1 remains open; a fresh independent L4 verdict is required after complete verification, followed by the mandatory human quiz-me Q&A only after APPROVE
- next: restore the previously used local `cl100k_base` cache without downloading or changing dependencies, rerun every required command, and submit the bounded correction for fresh independent L4 review

## iter 8 tokenizer-cache recovery and concurrency verification · 2026-09-02T13:35:00+00:00
- diagnosis: repository `/home/shubham/projects/conversational-memory-intelligence`; `.venv/bin/python` resolves to `/usr/bin/python3.12` and reports executable `/home/shubham/projects/conversational-memory-intelligence/.venv/bin/python`; installed `tiktoken==0.14.0`
- cache contract: installed `tiktoken.load.read_file_cached` checks `TIKTOKEN_CACHE_DIR`, then `DATA_GYM_CACHE_DIR`, then `<tempfile.gettempdir()>/data-gym-cache`; `cl100k_base` uses official source `https://openaipublic.blob.core.windows.net/encodings/cl100k_base.tiktoken`, URL SHA-1 cache filename `9b5ad71b2ce5302211f9c61530b329a4922fc6a4`, and required content SHA-256 `223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7`
- cache evidence: relevant cache environment variables were unset and Linux's effective default `/tmp/data-gym-cache` was absent; the earlier successful checkpoint recorded only Hugging Face/offline variables and did not record tiktoken cache state. The existing candidate `/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache/9b5ad71b2ce5302211f9c61530b329a4922fc6a4` is 1681126 bytes and its measured SHA-256 exactly matches the installed expected hash; `TIKTOKEN_CACHE_DIR` pointed verification processes read-only to that directory, and `tiktoken.get_encoding("cl100k_base")` loaded successfully. No cache file was copied or downloaded
- verification environment: `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`, `CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub`, `HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, and empty `CUDA_VISIBLE_DEVICES`
- architecture: `.venv/bin/python -m pytest tests/architecture -q` · 4 passed in 0.15s
- focused persistence/concurrency: `.venv/bin/python -m pytest tests/integration/infrastructure/test_faiss_index.py tests/integration/test_idempotent_admission.py tests/integration/test_partial_index_failure.py -q` · 43 passed in 12.50s
- locked M1 command: `.venv/bin/python -m pytest tests/architecture tests/unit tests/adapter tests/integration/test_admission_indexing.py tests/integration/test_first_slice.py tests/integration/test_idempotent_admission.py tests/integration/test_partial_index_failure.py tests/integration/test_restart_persistence.py tests/regression/test_context_budget.py -q` · 112 passed in 14.22s
- real-model command: `.venv/bin/python -m pytest tests/integration/test_real_model_first_slice.py::test_real_model_restart_owner_scope_and_bounded_context tests/integration/test_first_slice.py::test_real_faiss_persistence_failure_is_failed_and_nonretrievable -q -m real_model` · 2 passed in 9.50s; neither test skipped
- demo command: `.venv/bin/python -m conversational_memory.entrypoints.cli demo-first-slice` · exit zero using its temporary store; admission indexed/retrievable, replay identical, restart retrieval selected the admitted memory using 36 of 128 `cl100k_base` tokens, and the other owner received zero memories and empty context
- complete suite: `.venv/bin/python -m pytest tests -q` · 182 passed in 16.08s, including both real-model tests
- quality: Ruff passes; explicit-source mypy reports no issues in 22 source files; `pip check` reports no broken requirements with a non-failing unwritable pip-cache warning; CLI help exits zero and lists `demo-first-slice`; `git diff --check` exits zero with pre-existing CRLF normalization warnings
- preserved review limitations: the repository-requested verify/requesting-code-review skill remains unavailable; exact isolated editable installation remains unverified because build isolation requires unavailable PyPI; prior embedding-model cache provenance remains not independently reconstructed; the verified tokenizer prerequisite currently resides in Windows temporary storage and was not copied into a persistent Linux cache
- gate G4: pass · the bounded concurrency correction and all required offline Linux verification commands pass without production-code, test, dependency, or tokenizer-behavior changes during cache recovery
- gate G5: pending re-review · M1 remains open; a fresh independent L4 verdict is required, followed by the mandatory human quiz-me Q&A only after APPROVE
- next: submit the concurrency correction, regression evidence, verified cache prerequisites, and complete command evidence to a fresh independent L4 reviewer

## iter 8 L4 freeze-boundary rejection and approved exceptions · 2026-09-02T14:10:00+00:00
- gate G5: reject · the independent reviewer accepted the behavioral, concurrency, ranking, real-model, demo, graph, and quality evidence but found eight required supporting paths outside the literal locked M1 freeze boundary and did not independently execute the exact editable-install gate
- approved decision: the user authorized exactly eight M1-only path exceptions for mandatory checkpoint evidence, the approved implementation binding, shared SQLite/FAISS scenario collection, strict-boundary owner tests, context-budget test relocation, and two test package markers; `.genesis/decisions/M1-freeze-boundary-exceptions.md` records the purposes and is registered in the decisions manifest
- preserved boundary: locked `PLAN.md` and `DONE.html`, production code, tests, dependencies, and all other paths remain unchanged by this approval step; the exception decision does not broaden M1 behavior or authorize closure
- install gate: still pending · the eventual fresh independent reviewer, not the maker, must execute the exact `python -m pip install -e .` command under an approved isolated CPU-only setup and disclose any constraints; no download or installation was performed in this iteration
- gate G5: pending re-review · M1 remains open for independent installation evidence and a fresh verdict, followed by the mandatory human quiz-me Q&A only after APPROVE
- next: approve an isolated installation environment and explicit package-source/network permissions, then assign exact-gate execution and L4 review to an independent reviewer

## iter 8 freeze-record authorization and installation-evidence audit · 2026-09-02T14:45:00+00:00
- boundary authorization: the user explicitly authorized `.genesis/decisions/M1-freeze-boundary-exceptions.md` and `.genesis/decisions/decisions-manifest.md` in addition to the eight previously approved M1-only exceptions; the accepted decision now enumerates all ten exact paths and no others
- isolated installation transcript: from `/tmp/cmi-m1-l4.EgCxFx/repo`, the reviewer ran `export PATH=/tmp/cmi-m1-l4.EgCxFx/venv/bin:/usr/bin:/bin PIP_CACHE_DIR=/tmp/cmi-m1-l4.EgCxFx/pip-cache PIP_CONSTRAINT=/tmp/cmi-m1-l4.EgCxFx/torch-constraint.txt PIP_INDEX_URL=https://pypi.org/simple PIP_DISABLE_PIP_VERSION_CHECK=1; unset PIP_EXTRA_INDEX_URL; command -v python; python -c 'import sys; print(sys.executable)'; python -m pip install -e .`; the immediately printed interpreter was `/tmp/cmi-m1-l4.EgCxFx/venv/bin/python`, the constraint contained `torch==2.14.0+cpu`, and the editable target was exactly `.`. No command used `-e ..`; the second period seen in prose was punctuation
- isolated result evidence: the tool reported `Script completed`, pip completed dependency installation without an error, and subsequent isolated checks reported Python 3.12.3, pip 24.0 from the temporary environment, no broken requirements, package resolution to `/tmp/cmi-m1-l4.EgCxFx/repo/src/conversational_memory/__init__.py`, Torch `2.14.0+cpu`, no Torch CUDA version, unavailable CUDA, and successful CLI help. The transcript did not expose a numeric exit-code field, so an explicitly recorded exit status zero is not available and is not inferred
- working `.venv` incident and functional restoration: before PATH was explicitly rebound, the same literal `python -m pip install -e .` from `/tmp/cmi-m1-l4.EgCxFx/repo` used `/home/shubham/projects/conversational-memory-intelligence/.venv/lib/python3.12/site-packages`, uninstalled and reinstalled `conversational-memory-intelligence==0.1.0`, and temporarily pointed the working editable installation at the snapshot. From `/home/shubham/projects/conversational-memory-intelligence`, the reviewer then ran `PIP_CACHE_DIR=/tmp/cmi-m1-l4.EgCxFx/pip-cache PIP_NO_INDEX=1 PIP_DISABLE_PIP_VERSION_CHECK=1 .venv/bin/python -m pip install --no-build-isolation -e . && .venv/bin/python -c 'import conversational_memory, pathlib; print(pathlib.Path(conversational_memory.__file__).resolve())'`; it restored package resolution functionally to `/home/shubham/projects/conversational-memory-intelligence/src/conversational_memory/__init__.py`, but no pre-incident filesystem manifest exists and byte-for-byte `.venv` restoration is not established
- pip-cache incident: `/tmp/cmi-m1-l4.EgCxFx/venv/bin/python -m pip install --dry-run --no-deps --index-url https://download.pytorch.org/whl/cpu 'torch==2.14.0+cpu' -vv`, run from the temporary repository without `PIP_CACHE_DIR`, consulted and updated pip's default cache and used a cached 196.3 MB CPU Torch wheel. Exact affected cache files, before/after hashes or timestamps, and whether each artifact pre-existed are unavailable; no byte-level cache restoration is established
- post-restoration evidence: working `.venv/bin/python -m pip check` reported no broken requirements and `conversational_memory` resolved to the Linux repository; subsequent working-environment Ruff, explicit-source mypy, 112 locked M1 tests, two real-model tests, 182 full-suite tests, and the demo passed. These establish functional behavior, not exact environment restoration
- current read-only state: `/home/shubham/projects/conversational-memory-intelligence/.venv/bin/python` is the active executable with prefix `/home/shubham/projects/conversational-memory-intelligence/.venv`; `conversational_memory` resolves to `/home/shubham/projects/conversational-memory-intelligence/src/conversational_memory/__init__.py`; package metadata reports editable project location `/home/shubham/projects/conversational-memory-intelligence`; Torch is `2.14.0+cpu`, `torch.cuda.is_available()` is false, and `torch.version.cuda` is `None`; `.venv/bin/python -m pip check` reports no broken requirements with the existing unwritable pip-cache warning
- historical limitations: the unintended mutation and functional restoration are now documented, but exact `.venv` and default pip-cache restoration remain unknowable from the available evidence; the isolation violation is not erased or treated as an acceptance result
- current acceptance requirement: a fresh independent checker must execute the exact `python -m pip install -e .` gate in a correctly isolated environment, capture an explicit numeric zero exit status, confirm the intended repository/interpreter/package target and CPU-only constraints, and then issue a new L4 verdict. Historical byte-for-byte reconstruction of the prior `.venv` or pip cache is not substituted for that current gate
- gate G5: pending · M1 remains open; the rejected review and transcript evidence do not constitute APPROVE, and the mandatory human quiz remains after a future approval only
- next: have a fresh independent checker run and fully evidence the isolated exact install gate, re-evaluate the complete approved-boundary diff, and issue a new L4 verdict

## iter 8 fresh independent L4 approval · 2026-09-02T15:20:00+00:00
- gate G5: APPROVE · a new independent M1 L4 verification run approved the complete working tree; M1 remains open because the canonical post-APPROVE human quiz-me Q&A is still mandatory
- isolated snapshot: `/tmp/cmi-m1-l4.jtdgwL/repo`, 145 files and 111708744 bytes; fresh CPython 3.12.3 environment at `/tmp/cmi-m1-l4.jtdgwL/venv`; an isolated pip cache was asserted before every pip operation
- exact install gate: immediately before literal `python -m pip install -e .`, executable and `sys.prefix` pointed to the temporary venv, the constraint was `torch==2.14.0+cpu`, and the editable target was the snapshot repository; numeric exit status was 0 and the installed package resolved to snapshot source
- installation constraints: Torch reported `2.14.0+cpu`, CUDA build `None`, and CUDA unavailable. The accepted run used the previously recorded local CPU wheel and contacted only authorized PyPI hosting; pip cache size was 156505902 bytes and total temporary storage was 1943524245 bytes, within approved limits
- verification: `pip check` passed; CLI help exited zero; Ruff passed; explicit-source mypy reported no issues in 22 files; locked M1 tests passed 112; both required real-model tests passed without skips; demo exited zero and proved restart, identical replay, owner isolation, and 34/128-token bounded context; complete suite passed 182; `git diff --check` exited zero with existing CRLF warnings; context graph remained valid, acyclic, and satisfied its ownership/downstream invariants
- boundary and mutation evidence: every working-tree path fits the locked M1 boundary plus the ten exact approved exceptions; installation changed neither snapshot nor working source manifests, and post-gate snapshot differences were limited to generated mypy/Ruff/pytest/bytecode caches. The working `.venv` still targeted the working repository, passed `pip check`, and retained CPU Torch
- preserved historical incident: an earlier attempt in this review was stopped after PyTorch redirected to unauthorized `download-r2.pytorch.org`; that attempt's exact-install status was 1. The earlier working-`.venv` and default-pip-cache mutation incident, functional restoration evidence, and unresolved byte-level restoration limitations remain recorded separately and are not erased by this approval
- optional explain-diff: skipped at the user's direction because it would create a repository artifact; this optional gate does not block the milestone
- gate status: L4 is approved, but M1 is not closed; the three canonical quiz-me questions must be asked one at a time, answered by the human, assessed honestly, and logged before closure
- next: ask quiz question 1 (design decision), wait for the human's answer, and do not proceed to question 2 until it is assessed

## M1 post-APPROVE human quiz · in progress
- Q1 · design decision: Why does M1 obtain the owner-scoped, indexed vector-ID allowlist from authoritative SQLite before searching FAISS, instead of searching FAISS globally and filtering afterward?
- Human answer: SQLite first supplies only the vector IDs belonging to that user and ready for retrieval, so another user's memories do not enter FAISS results. SQLite is trusted for ownership and readiness while FAISS only searches matching vectors; global search followed by filtering carries greater cross-owner leakage risk. Reduced search work is secondary to isolation.
- Assessment: pass · correctly identifies SQLite authority, derived FAISS responsibility, pre-search owner/index-state enforcement, fail-closed isolation purpose, and the secondary performance benefit
- Next quiz step: Q2 · edge case
- Q2 · edge case: What happens if FAISS successfully publishes and verifies a vector generation, but the following SQLite acknowledgement fails before the memory can be marked `indexed`? What should retrieval and a later identical retry do?
- Human answer: SQLite remains `pending`; the vector may exist in FAISS but retrieval must exclude it because SQLite is authoritative. A later identical request reuses the stored memory, embedding, and stable vector ID, completes indexing/acknowledgement without creating or embedding a new memory, and becomes retrievable only after SQLite confirms `indexed`.
- Assessment: pass · correctly explains the non-retrievable pending state, SQLite authority, stable-work reuse, duplicate prevention, and the acknowledgement condition for retrieval
- Next quiz step: Q3 · change impact
- Q3 · change impact: The concurrency correction introduced both a process-wide admission lock and a reload of the verified durable FAISS generation before publication. What distinct failure does each mechanism prevent, especially when multiple service/adapter instances share one store?
- Human answer: the process-wide lock prevents two admissions or retries from concurrently passing idempotency resolution and executing the mutation workflow, which could cause incidental failure or duplicate work. The FAISS reload prevents an adapter's stale in-memory index from overwriting a newer generation published by another instance; cloning the latest verified finals retains the other instance's vectors.
- Assessment: pass · correctly distinguishes workflow serialization from derived-index instance coherence and identifies the concrete race and lost-vector failure each prevents
- Quiz result: pass · all three required categories—design decision, edge case, and change impact—were answered by the human and assessed as correct
- Milestone state: remains open by explicit user instruction; the L4 APPROVE and complete Q+A gate are recorded, but no closure action has been taken

## M1 closure · 2026-09-02
- authorization: the user explicitly authorized formal M1 closure
- completion evidence: independent L4 G5 verdict APPROVE; exact isolated editable-install exit status 0; locked M1 tests 112 passed; required real-model tests 2 passed without skips; complete suite 182 passed; demo, architecture, Ruff, mypy, pip consistency, CLI help, diff check, approved freeze boundary, and context-graph invariants verified
- human gate: all three canonical quiz-me answers were supplied by the human and assessed as pass
- preserved record: all earlier L4 rejections, the unauthorized-host stopped attempt, the working `.venv` and default pip-cache incident, functional restoration, byte-level uncertainty, tokenizer-cache location, unavailable review skill, and embedding-cache provenance limitation remain recorded above and are not erased by closure
- milestone result: M1 complete
- next milestone: M2 · Current-State Filtering; not started
