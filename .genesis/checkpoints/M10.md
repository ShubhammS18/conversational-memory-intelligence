# M10 — Complete Fixed-Workload Evaluation

- Status: BUILD iteration 4 complete; M10 open; later iterations unauthorized
- Date: 2026-09-18
- Depends on: closed M9; M1-M9 accepted behavior remains authoritative
- L4: not started
- Human quiz: not started
- Closure: not authorized

## G0 Existence Pre-Flight

- Canonical evidence: Genesis, LOOPS, AGENT-ADAPTERS, PLAN M10, DONE,
  CURRENT, M1-M9 closure checkpoints, manifest, implementation notes,
  context graph, relevant design/ADRs, baseline protocol/results, and D4 evaluation.
- Wiki: Fixed-Workload Evaluation index pointers; Evaluation-Frameworks and
  Ruthless-Testing under /mnt/c/Users/Asus/Desktop/agentic-swe-kit/wiki.
- Skills used: agentic-swe-master, modular-architecture, production-readiness,
  llmops-ai-agents, engineering-mindset.
- Existence evidence: six original workloads and recorded baseline exist;
  real integrated service/composition and nine milestone demos are reusable.
  tests/evaluation and evaluate-fixed-workload are not yet implemented.
- Verdict: PARTIAL — extend the completed integrated system with evaluation;
  do not rebuild memory behavior or activate BUILD through this checkpoint.
- Repository: /home/shubham/projects/conversational-memory-intelligence.
- Baseline HEAD: bbd6fb6 feat(m9): add privacy-safe observability.
- Preflight git status --short: empty; no tests, installations, or edits ran
  during the read-only proposal.
- M9 closure execution record: 221 locked, 4 architecture, 666 full offline;
  independent L4 APPROVE and quiz PASS 3/3. These were not rerun at G0.
- Binding: ../decisions/M10-fixed-workload-evaluation-bindings.md.
- Exact boundary: ../decisions/M10-freeze-boundary-exceptions.md.

## Approved sequence after separate BUILD authorization

1. Regression-first loader/report/serializer slice in entrypoints/evaluation.py
   and tests/evaluation/test_fixed_workload.py; empty package marker if needed.
2. Real fixture adaptation, deterministic execution, and parameterized checks.
3. Restart, deletion, zero-budget, repeatability, and recursive privacy proofs.
4. Shared evaluate-fixed-workload CLI/parser and full maker verification.
5. Fresh independent L4, human quiz, then separately authorized closure.

## Future verification prerequisites and commands — not executed

Use .venv/bin/python; every model-dependent command receives:
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache

- .venv/bin/python -m pytest tests/evaluation tests/regression tests/architecture -q
- .venv/bin/python -m pytest tests/architecture -q
- .venv/bin/python -m pytest tests -q
- .venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload
- .venv/bin/python -m ruff check src tests
- .venv/bin/python -m mypy src/conversational_memory
- .venv/bin/python -m pip check
- .venv/bin/python -m conversational_memory.entrypoints.cli --help
- Context graph: valid JSON, resolved endpoints, acyclic edges, preserved invariants.
- git diff --check

No verification result is claimed. Require actual real-model execution without
skips and verified cl100k_base cache before offline evaluation.
Preserve historical installation-isolation incidents and independent install
evidence, cache-provenance limitations, temporary Windows tokenizer location,
unavailable review skill, and process-only locking. No gate is waived.
Next action: await explicit authorization for first bounded BUILD iteration.

## BUILD iteration 1 — fixed inputs and safe reports · 2026-09-18

- Authorization: user explicitly activated M10 and authorized only iteration 1.
  The previously approved/applied governance binding is effective; no later
  execution, orchestration, CLI, review, or closure is authorized.
- G1: skills considered/used: agentic-swe-master, modular-architecture,
  production-readiness, llmops-ai-agents. Standard-library entrypoint contracts
  preserve inward dependencies and require no production-pipeline changes.
- G2: added entrypoints/evaluation.py, test_fixed_workload.py, and an empty
  tests/evaluation/__init__.py marker. Added 102 focused contract/input tests.
- Regression-first: tests preceded the module. Exact command
  `.venv/bin/python -m pytest tests/evaluation/test_fixed_workload.py -q`
  initially exited 2: ModuleNotFoundError for the new evaluation module;
  1 collection error in 0.21s. Initial implementation: 56 passed in 0.35s.
- Additional negative regressions preceded their correction: the same command
  returned 2 failed, 100 passed in 0.64s (exit 1), showing PASS/PARTIAL could
  omit mandatory checks. Required-check/profile enforcement corrected this.
- Final focused command: `.venv/bin/python -m pytest tests/evaluation/test_fixed_workload.py -q`
  — 102 passed in 0.57s, exit 0.
- `.venv/bin/python -m ruff check src tests` — All checks passed!, exit 0.
- `.venv/bin/python -m mypy src/conversational_memory` — Success: no issues
  found in 32 source files, exit 0. An earlier run found three redundant casts
  in the new module; removed those casts without weakening validation.
- `git diff --check` — exit 0, no output; checked again after evidence updates.
- `sha256sum experiments/naive_baseline/workload/*.json experiments/baseline_protocol.md experiments/baseline_results.csv`
  — all eight hashes match the approved binding, unchanged from G0.
- Loader: exact six-file discovery/order, closed nested schemas, SHA-256
  verification before parsing, original bytes/turns/fillers retained, immutable
  internal records, and redacted fixture/baseline failure codes.
- Report: frozen/deeply immutable contracts, strict field types, closed case/
  verdict/reason/check vocabularies, exact settings and baseline metadata,
  mandatory nonfailure checks, and truthful unexecuted null evidence.
- Serializer: revalidates all nested report fields, emits only fixed allowlisted
  metadata, canonical UTF-8 JSON with newline, stable ordering, and derived
  totals. Tests scan nested objects and JSON/bytes for fixture-derived sensitive,
  owner, query, and content sentinels; forged frozen fields are rejected.
- Synthetic reports in unit tests are contract examples, not workload execution.
  No embeddings, admissions, retrieval, FAISS operations, startup composition,
  CLI wiring, real-model runs, or evaluation execution occurred.
- G3: no actual token-usage counter is available; no usage figure is invented.
- G4: focused tests, Ruff, explicit-source mypy, and diff checks passed.
- G5: deferred; no independent L4 started, no quiz or closure claimed.
- Pre-existing governance work was preserved. Original workload/baseline,
  service/domain/adapters, dependencies, migrations, PLAN/DONE, context graph,
  implementation notes, and completed-milestone tests remain unchanged.
- Historical incidents and limitations remain as recorded above.
- Next: stop; await separate authorization for the next bounded iteration.

## BUILD iteration 2 prerequisite check — blocked · 2026-09-18

- Authorization: real typed adaptation and primary admissions/CURRENT retrieval
  only; restart, zero-budget, supplemental forgetting, CLI, and later work remain
  excluded. No iteration-2 implementation or tests have been added yet.
- Before model-dependent work, inspected the installed tiktoken cl100k_base
  constructor and checked the recorded cache file read-only. Constructor URL:
  https://openaipublic.blob.core.windows.net/encodings/cl100k_base.tiktoken.
- Expected SHA-256 from that installed constructor:
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
- Recorded file is unavailable:
  /mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache/9b5ad71b2ce5302211f9c61530b329a4922fc6a4.
  No actual checksum can be reported for a missing file.
- TIKTOKEN_CACHE_DIR and DATA_GYM_CACHE_DIR are unset in this process.
  Targeted checks also found no matching file in /tmp/data-gym-cache,
  /home/shubham/.cache/conversational-memory-intelligence-m1/tiktoken, or
  /home/shubham/.cache/data-gym-cache. No whole-drive search occurred.
- Recorded Hugging Face hub directory exists; this does not supply tiktoken
  encoding data or establish full model snapshot verification for this run.
- No tokenizer load/download, model execution, installation, migration, source/
  test change, focused test run, Ruff run, or mypy run occurred in iteration 2.
  Iteration-1 passing evidence remains historical and is not relabeled as new.
- Stop: require an accessible existing cache file verified against the expected
  hash, or separate explicit permission for official-source acquisition. No
  tokenizer fallback, production behavior change, or evaluation-gate waiver.
- Primary-only evidence must not claim final M10 PASS/PARTIAL checks requiring
  restart and supplemental work; those remain deferred when work resumes.

## BUILD iteration 2 — real primary paths · 2026-09-18

- Authorization: resume iteration 2 only after the separately authorized official
  tokenizer download; no restart/repeatability, zero-budget, supplemental
  forgetting, CLI, pipeline, dependency, or migration changes.
- Cache prerequisite: recorded cl100k_base file now exists. Read-only sha256sum
  equals the installed constructor's expected value exactly:
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
  Recorded directory: /mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache.
  Official acquisition history is separate from this offline execution; no
  tokenizer/model download occurred during iteration 2.
- G1: agentic-swe-master, modular-architecture, production-readiness, and
  llmops-ai-agents guided scope, dependency boundaries, and case-specific evidence.
- G2: extended only entrypoints/evaluation.py; added
  tests/evaluation/test_primary_execution.py (31 tests, including six explicitly
  parameterized real-model primary cases); existing iteration-1 tests preserved.
- Regression-first: new primary tests ran before the implementation and returned
  6 failed, 8 errors in 0.82s, exit 1: adapt_case and execute_primary_workloads
  did not yet exist. No primary operations executed in that failing run.
- Initial real primary run: 14 passed in 34.48s, exit 0, no skips.
- Final focused command, run with the exact process-local environment below:
  `.venv/bin/python -m pytest tests/evaluation -q -s`
  — 133 passed in 25.60s, exit 0, no skips.
- Environment for every model-dependent test command:
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache.
- `.venv/bin/python -m ruff check src tests` — All checks passed!, exit 0.
  Earlier new-code pairwise/import-order findings were corrected.
- `.venv/bin/python -m mypy src/conversational_memory` — Success: no issues
  found in 32 source files, exit 0.
- `git diff --check` — exit 0; repeated after mandatory evidence updates.
- Input preservation: tests compare all eight original fixture/baseline bytes
  before/after execution, and sha256sum still matches every approved binding hash.
- Real execution: one initial local composition per case, sequential original
  candidate admissions, frozen opaque IDs/owners/timestamps/metadata, real
  cache-only CPU MPNet, real temporary durable SQLite/FAISS, explicit 0.50,
  limit 10, budget 128, and exactly one primary CURRENT retrieval per case.
- Primary results (not final M10 acceptance):
  - case1: PARTIAL, expected_memory_not_selected; no_relevant_memory; 0 tokens.
  - case2: PARTIAL, expected_memory_not_selected; no_relevant_memory; 0 tokens.
  - case3: PARTIAL, expected_memory_not_selected; no_relevant_memory; 0 tokens.
  - case4: PASS; memories_selected; m10-case4-c0000; 20/128 tokens.
  - case5: PASS; no_relevant_memory; 0 tokens; sensitive candidate rejected;
    fifteen fillers admitted, sixteen admission attempts, no forgetting calls.
  - case6: PASS; no_relevant_memory; 0 tokens with seventeen owned candidates.
- All primary hard safety checks pass. Tests inspect real persisted owner scope,
  one-to-one mappings/embeddings, exact bidirectional supersession chains, and
  sensitive absence; no threshold, budget, content, fixture, or assertion tuning.
- Intermediate PrimaryCaseEvidence is deliberately separate from final
  EvaluationReport. Its deterministic case-data array contains only allowlisted
  fields; recursive object/JSON/byte scans pass. It omits unperformed checks and
  is rejected by the final-report serializer. Library chatter is discarded.
- Case5's original control turn is retained as deferred metadata, not admitted
  or executed. control_actions=0 accurately records this iteration's scope.
- No fresh restart, repeated retrieval, zero-budget query, historical query,
  forgetting supplement/control invocation, CLI wiring, or final acceptance
  report ran. Test-suite verification reruns are not repeatability probes.
- G3: actual token-usage counter unavailable; no fabricated usage count.
- G4: focused execution/contracts, Ruff, mypy, input hashes, and diff check pass.
- G5: deferred. No independent L4, quiz, closure, or production claim.
- Existing governance work and all M1-M9 historical incidents/limitations remain
  preserved. No source outside entrypoints, original inputs, completed-milestone
  tests, dependencies/migrations, PLAN/DONE, or context graph changed.
- Stop: M10 open; await separate authorization for the next bounded iteration.

## BUILD iteration 3 — approved supplemental probes · 2026-09-18

- Authorization: supplemental verification only; frozen integrated system,
  original inputs, settings, and verdict rules unchanged. No CLI wiring or
  source changes outside entrypoints; no installs, new migrations, or downloads.
- G1: agentic-swe-master, modular-architecture, production-readiness, and
  llmops-ai-agents guided regression-first scope, privacy, and truthful evidence.
- G2: extended entrypoints/evaluation.py with execute_verified_workloads and
  approved probes; added tests/evaluation/test_supplemental_execution.py
  (15 tests). Preserved primary-only runner and all earlier tests/evidence.
- Regression-first command, with the process-local environment below:
  `.venv/bin/python -m pytest tests/evaluation/test_supplemental_execution.py -q`
  — 6 failed, 9 errors in 0.68s, exit 1, before supplemental implementation;
  execute_verified_workloads and _classify_completed were absent.
- Initial passing supplemental command: same command — 15 passed in 37.54s,
  exit 0, no skips. Strengthened clock-use and exact zero-budget assertions
  before final focused verification.
- Final focused command:
  `.venv/bin/python -m pytest tests/evaluation -q -s`
  — 148 passed in 47.56s, exit 0, no skips; actual real-model execution.
- Exact process-local environment for every model-dependent command:
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache.
- `.venv/bin/python -m ruff check src tests` — All checks passed!, exit 0.
  New-code loop-capture findings were corrected with explicitly bound defaults.
- `.venv/bin/python -m mypy src/conversational_memory` — Success: no issues
  found in 32 source files, exit 0. New-code Any-return finding corrected.
- `git diff --check` — exit 0, repeated after mandatory evidence updates.
- Cache: read-only sha256sum for the recorded cl100k_base file equals
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7;
  runner independently checks this approved hash before tokenizer use.
- Input preservation: all six fixture SHA-256 values and both baseline hashes
  still match the binding. Tests compare all eight original byte sequences
  before/after execution. No source artifact rewritten or report saved in repo.
- Real cache-only CPU MPNet, revision e8c3b32edf5434bc2275fc9bab85f82640a19130,
  dimension 768; separate real temporary SQLite/FAISS stores, original sequential
  admissions, owners, IDs, timestamps, and explicit relationship declarations.
- Normal composition/startup recovery runs on every fresh service. Primary
  retrieval follows a fresh composition; repeat follows another fresh composition.
  Five-table SQLite snapshots match before/after restarts and retrieval. Exact
  tuple (outcome, ordered IDs, ordered exclusions, tokens_used, context) matches
  for all six cases; no ID-only or context-free repeatability substitute.
- Primary outcomes preserved: cases1-3 PARTIAL, expected_memory_not_selected,
  no_relevant_memory, 0 tokens; case4 PASS, expected ID selected, 20/128 tokens;
  cases5-6 PASS, no_relevant_memory, 0 tokens. All six restart_verified and
  repeatable flags true. No tuning or expectation changes.
- Zero-budget original-query probes: all return empty memories/context/IDs and
  0 tokens. Case4 returns budget_excluded with budget_exceeded exclusions;
  cases1-3/5-6 retain no_relevant_memory and exact primary exclusion tuples.
  Each current retrieval reads the lifecycle clock exactly once.
- Sensitive candidate rejected before embedding, ID allocation, or persistence;
  event stages and unchanged SQLite snapshot establish no indexing/mutation.
  Original case5 control executes at its source position as opaque not_found,
  without lifecycle-clock or FAISS work and without SQLite state changes. Never
  seed the rejected credential; sixteen attempts, fifteen accepted, one rejected,
  one control action accurately reported, distinct from baseline accounting.
- Admitted case5 f0000 forgotten at T0+2 days; completion uses T0+2 days+1 second.
  Persisted tombstone/complete cleanup and removal of its mapping verified.
  Fresh restart then queries its original text with CURRENT and HISTORICAL,
  limit 10, budget 128: deleted memory absent in both; historical reads no clock.
  Post-forgetting five-table snapshot unchanged through restart/both queries;
  no reactivation or relationship mutation. Every supplemental context safety/
  whole-block/exact-token/budget check passes.
- Completed CaseReport/EvaluationReport uses existing strict contracts and
  deterministic privacy-safe serializer. Recursive object/JSON/UTF-8 scans and
  fixture-derived content/query/identity/credential/key sentinel checks pass.
  Reports contain only closed allowlisted fields; no raw context or event dump.
- Hard supplemental/state/safety failures classify FAIL before quality omissions;
  failed repeatability uses non_reproducible_result. Tests prove failures cannot
  be averaged or downgraded into PARTIAL. Totals: 3 PASS, 3 PARTIAL, 0 FAIL;
  no compensating aggregate score or production claim.
- G3: actual token-usage counter unavailable; no fabricated count.
- G4: focused real execution, original bytes/hashes, Ruff, mypy, diff check pass.
- G5: deferred. CLI, full locked/comprehensive maker gates, fresh independent L4,
  human quiz, and closure remain unperformed/unauthorized by this iteration.
- Existing governance work and historical incidents/limitations remain preserved:
  installation isolation history, unreconstructed embedding-cache provenance,
  Windows-temp tokenizer location, unavailable review skill, process-only locks.
- Stop: M10 open; await separate authorization. No staging, commits, pushes,
  post-M10 work, dependency/migration changes, or completed-milestone edits.

## BUILD iteration 4 — fixed-workload CLI · 2026-09-18

- Authorization: wire evaluate-fixed-workload to the existing verified runner;
  no policy tuning, non-entrypoint source changes, installs, or production claims.
- G1: agentic-swe-master, modular-architecture, production-readiness, and
  llmops-ai-agents guided regression-first scope and privacy-safe boundary handling.
- G2: entrypoints/cli.py adds parser/help/dispatch, isolated diagnostic handling,
  temporary case-store allocation, and documented exit policy. Extended only
  entrypoints/evaluation.py with safe progress observations and closed failure
  report construction; existing adaptation/probes/settings/verdicts unchanged.
  Added tests/evaluation/test_evaluation_cli.py (15 tests); earlier tests preserved.
- Regression-first: with the exact process-local environment below,
  `.venv/bin/python -m pytest tests/evaluation/test_evaluation_cli.py -q`
  — 11 failed in 2.99s, exit 1, before CLI wiring; command was unknown, including
  the real subprocess command. No evaluation ran in that red subprocess.
- Additional privacy regression: unsupported CLI argument echoed an injected
  private sentinel through argparse. Focused command selecting this regression
  and the primary-evidence failure test returned 1 failed, 1 passed,
  11 deselected in 0.46s, exit 1. Command-specific argument rejection now emits
  only safe prerequisite output, without echoing supplied values.
- Initial complete focused run: 159 passed in 80.00s; subsequent run including
  argument/privacy and retained-primary evidence tests: 161 passed in 72.24s.
  Added late fixture/baseline integrity assertions before final verification.
- Final focused command:
  `.venv/bin/python -m pytest tests/evaluation -q`
  — 163 passed in 70.26s, exit 0, no skips; includes real offline CLI subprocess,
  all six primary paths, restarts, repeatability, zero-budget, and forgetting probes.
- CLI-only non-model checks:
  `.venv/bin/python -m pytest tests/evaluation/test_evaluation_cli.py -q -m 'not real_model'`
  — 14 passed, 1 deselected in 0.55s, exit 0. Deselected real CLI separately
  executed in the complete focused run and the literal standalone command.
- Exact process-local environment for every model-dependent command:
  HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache.
- Literal standalone command with that environment:
  `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload`
  — numeric exit 0. Printed one compact, sorted UTF-8 JSON object plus newline,
  schema_version=1, implementation_stage="pre-production reference implementation",
  policy_id=M10-fixed-workload-v1, historical baseline/comparison notes, exact frozen
  settings, all six case-specific checks/reasons/results, and unweighted totals.
- Real CLI results: cases1-3 PARTIAL / expected_memory_not_selected /
  no_relevant_memory / 0 tokens; each expected ID appears as an authorized
  below_relevance_threshold exclusion. Case4 PASS / expected_behavior_confirmed /
  memories_selected / m10-case4-c0000 / 20 tokens. Cases5-6 PASS /
  expected_behavior_confirmed / no_relevant_memory / 0 tokens. Every case
  executed=true, repeatable=true, restart_verified=true. Totals: 3 PASS,
  3 PARTIAL, 0 FAIL; hard checks true, expected-primary quality checks false
  only for cases1-3. No tuning or expectation changes to manufacture green.
- Frozen settings remain real CPU MPNet at immutable revision
  e8c3b32edf5434bc2275fc9bab85f82640a19130, dimension 768, threshold 0.50,
  limit 10, primary budget 128, cl100k_base, fixed time and seed. No CLI knobs.
- Exit policy documented in subcommand help and tested: 0 for completed
  evaluation without FAIL (PARTIAL remains visible); 1 for completed hard FAIL
  or unexpected/safety execution failure; 2 for prerequisite failure. Ordinary
  diagnostic/exception text suppressed; process-control BaseException not caught.
- Failure output uses strict existing report/serializer contracts and closed
  reasons/checks. Retains actual recorded counts/control actions and primary
  results if failure follows execution; incomplete future cases have no invented
  outcome/context/token evidence. Previously completed cases remain recorded;
  late fixture/baseline integrity failure forces hard FAIL and false byte checks.
  No weighted score, failure downgrade, skipped case, or fabricated success.
- CLI tests validate deterministic JSON formatting and recursively scan objects,
  JSON strings, and UTF-8 bytes for forbidden fields and original fixture/query/
  content/user/credential/HMAC-key sentinels. Real subprocess stderr empty.
  Fake chatter/error/private argument sentinels absent from failure output.
- `.venv/bin/python -m ruff check src tests` — All checks passed!, exit 0.
  Deliberate ordinary-exception redaction boundary documented locally for BLE001.
- `.venv/bin/python -m mypy src/conversational_memory` — Success: no issues
  found in 32 source files, exit 0; new results-list annotation fixed initial finding.
- `.venv/bin/python -m conversational_memory.entrypoints.cli --help` — exit 0,
  evaluate-fixed-workload listed, existing commands preserved.
- `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload --help`
  — exit 0; explicit offline/pre-production/PARTIAL/0/1/2 policy, no tuning options.
- `git diff --check` — exit 0, repeated after mandatory evidence updates.
- Input preservation: real CLI test compares all six original workload and both
  baseline byte sequences before/after; SHA-256 checks still match every approved
  binding value. Runner reloads/verifies inputs after execution. Tokenizer cache
  SHA-256 remains 223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
- G3: actual token-usage counter unavailable; no fabricated count.
- G4: requested focused/model/CLI/help/privacy/hash/lint/type/diff checks pass.
- G5: deferred. Complete locked/full maker verification, fresh independent L4,
  human quiz, and closure remain pending; this CLI result is not their substitute.
- Pre-existing governance work, earlier evidence, and historical incidents/
  limitations preserved: installation isolation, unreconstructed model-cache
  provenance, Windows-temp tokenizer cache, unavailable review skill, process-only
  locking. No dependency, new migration, fixture/baseline, completed-milestone test,
  PLAN/DONE/context graph/implementation-notes change. No report saved in repo.
- Stop: M10 open; later iteration/review/closure awaits separate authorization.
  No staging, commits, pushes, or post-M10 work.

## Complete M10 maker verification · 2026-09-18

- Authorization: maker verification only; no new behavior, settings/policy tuning,
  fixture/baseline changes, dependency installation, or independent L4 start.
- Repository: /home/shubham/projects/conversational-memory-intelligence.
  Actual executable: /home/shubham/projects/conversational-memory-intelligence/.venv/bin/python.
- G1: agentic-swe-master, modular-architecture, production-readiness, and
  llmops-ai-agents used for evidence-only lifecycle, architecture, and evaluation
  gates. Existing approved workload/adaptation/verdict rules remain authoritative.
- Each locked/full/model evaluation command used this process-local prefix:

  ```bash
  env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES= CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  ```

- `.venv/bin/python -m pytest tests/evaluation tests/regression tests/architecture -q`
  — 179 passed in 73.90s, exit 0; no skips or deselections.
- `.venv/bin/python -m pytest tests -q`
  — 829 passed in 103.57s, exit 0; no skips or deselections, including real-model
  and real fixed-workload execution. No markers were used to exclude gates.
- `.venv/bin/python -m pytest tests/architecture -q`
  — 4 passed in 0.42s; architecture remains unchanged.
- `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload`
  — real offline CPU execution, numeric exit 0; one structured JSON report,
  pre-production reference implementation notice, and no diagnostic chatter.

  | Case | Verdict | Reason | Primary outcome | Selected IDs | Tokens / budget |
  | --- | --- | --- | --- | --- | --- |
  | case1 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case2 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case3 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case4 | PASS | expected_behavior_confirmed | memories_selected | m10-case4-c0000 | 20 / 128 |
  | case5 | PASS | expected_behavior_confirmed | no_relevant_memory | none | 0 / 128 |
  | case6 | PASS | expected_behavior_confirmed | no_relevant_memory | none | 0 / 128 |

- Totals: 3 PASS, 3 PARTIAL, 0 FAIL. All six executed, repeatable, and
  restart_verified. All applicable hard safety/state, zero-budget, input-byte,
  privacy, ownership, lifecycle, relevance, and exact-token checks true.
  Only expected_primary_ids is false for cases1-3; their required useful IDs
  appear as below_relevance_threshold exclusions. Their quality limitations
  remain visible, not averaged away or converted to PASS. Case5 sensitive
  rejection, opaque rejected-target lookup, completed forgetting, current/history
  deletion exclusion, and no-reactivation checks all true.
- CLI exit 0 is correct: approved policy permits completed quality PARTIALs
  when there are no FAIL safety/state/evaluation outcomes. This is not universal
  recall success, production readiness, or independent approval.
- Frozen settings confirmed by real report: CPU MPNet revision
  e8c3b32edf5434bc2275fc9bab85f82640a19130, dimension 768, threshold 0.50,
  limit 10, 128-token primary budget, cl100k_base, frozen_time
  2026-01-02T00:00:00Z, seed 10. No tuning or new fallback.
- `.venv/bin/python -m conversational_memory.entrypoints.cli --help`
  — exit 0; evaluate-fixed-workload listed and prior commands preserved.
- `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload --help`
  — exit 0; explicit pre-production/offline requirements and exit 0/1/2 policy.
- `.venv/bin/python -m ruff check src tests`
  — All checks passed!, numeric exit 0.
- `.venv/bin/python -m mypy src/conversational_memory`
  — Success: no issues found in 32 source files, numeric exit 0.
- `.venv/bin/python -m pip check`
  — No broken requirements found, numeric exit 0. Non-failing warning: default
  /home/shubham/.cache/pip or its parent is not owned/writable, so pip disabled
  that cache. No installation, permission repair, or cache repair attempted.
- SHA verification before and after standalone real CLI: `.venv/bin/python -B -c`
  read the six sorted `experiments/naive_baseline/workload/case*.json` files,
  `experiments/baseline_protocol.md`, and `experiments/baseline_results.csv`,
  computed hashlib.sha256 over bytes, extracted the eight approved 64-hex
  values from M10-fixed-workload-evaluation-bindings.md with
  `re.findall(r"[0-9a-f]{64}", binding)`, and asserted exact list equality and
  eight files/values. Both commands exited 0: 8/8 match. Source fixtures and
  baselines remain byte-identical to approved inputs; no baseline writer ran.
- `sha256sum /mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache/9b5ad71b2ce5302211f9c61530b329a4922fc6a4`
  — exit 0; verified cl100k_base SHA-256
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
  Offline runs used this existing cache; no tokenizer/model downloads.
- Context graph validation: `.venv/bin/python -B -c` used stdlib JSON/Path,
  checked unique node/edge records, every edge's two resolved endpoints, all
  nine nonempty named invariant strings, and Kahn topological traversal visiting
  every node. Numeric exit 0: 36 nodes, 43 edges, 9 invariant records, acyclic.
  Structural checks do not replace the behavioral safety/architecture suites.
  No graph metadata or topology changed.
- Installed versions read through importlib.metadata: Python 3.12.3,
  pip 26.2.1, torch 2.14.0+cpu, sentence-transformers 5.7.0, faiss-cpu 1.15.0,
  tiktoken 0.14.0, pydantic 2.13.5, pytest 9.1.1, Ruff 0.16.5, mypy 1.20.2.
- `git diff --check` — numeric exit 0, no output; repeated after checkpoint edits.
- G2: quantified evidence for 179 locked and 829 full tests plus all six real
  cases recorded; task edits limited to M10/CURRENT mandatory evidence.
- G3: actual token-usage counter unavailable; no fabricated count.
- G4: complete requested maker gates passed. G5: pending; fresh independent
  L4 not started, human quiz not started, closure not authorized. M10 remains
  open and is ready for separately authorized fresh independent L4 review.
- Historical installation-isolation incidents/evidence and existing limitations
  remain preserved: unreconstructed model-cache provenance, Windows-temporary
  tokenizer location, unavailable review skill, process-only locking. These gates
  do not resolve them. Prior source/tests/governance work preserved; no changes
  to fixtures, baselines, settings, dependencies, PLAN/DONE, implementation notes,
  or context graph; no staging, commit, push, or post-M10 work.

## Fresh independent L4 APPROVE · 2026-09-18

- Source: full independent M10 L4 report supplied by the user; reproduced below.
  Reviewer file/line references are preserved as supplied, not newly reconstructed.
- Current L4 verdict: APPROVE, no blocking findings. Earlier G5-pending entries
  remain historical evidence. M10 remains open; human quiz not started and
  explicit closure authorization still required.
- No verification rerun or implementation change was performed to record this
  report. Independent input-hash inspection below belongs to the reviewer;
  maker execution counts/results are historical records reviewed, not rerun.
- Skills: agentic-swe-master for lifecycle evidence bookkeeping only.

### Full supplied independent reviewer report

APPROVE

Fresh independent L4 review completed in strictly read-only mode. No files were
edited, tests rerun, dependencies installed, or closure work started.

The review applied the canonical L4 lifecycle (.genesis/LOOPS.md:244), locked
PLAN/DONE, M10 binding and freeze decision, checkpoint/CURRENT, and all nine
context-graph invariants.

- Original inputs preserved: independently computed SHA-256 values for all six
  fixtures and both baseline artifacts match the eight approved binding values
  exactly. The loader verifies exact fixture discovery, order, hashes, and
  baseline rows; execution verifies inputs again afterward. Tests compare
  original bytes before and after real execution. Loader
  (src/conversational_memory/entrypoints/evaluation.py:302), preservation tests
  (tests/evaluation/test_primary_execution.py:58).

- Frozen real execution: the integrated runner uses separate temporary durable
  SQLite/FAISS stores, original candidate order and queries, explicit CURRENT
  intent, trusted evaluation identities and declared supersession links.
  Execution requires offline settings and a SHA-verified tokenizer cache.
  Existing composition loads cache-only CPU MPNet at revision
  e8c3b32edf5434bc2275fc9bab85f82640a19130, dimension 768; threshold is 0.50,
  limit 10, primary budget 128, tokenizer cl100k_base, and primary time is fixed.
  No tuning options or inferred extraction were introduced. Adaptation
  (src/conversational_memory/entrypoints/evaluation.py:556), runner
  (src/conversational_memory/entrypoints/evaluation.py:834).

- Deterministic, private reporting: immutable contracts enforce strict types,
  closed reasons/checks/outcomes, planned case-local IDs, mandatory nonfailure
  checks, and exact frozen settings. Serialization revalidates nested fields
  and emits compact sorted JSON plus newline, with case-specific evidence and
  unweighted totals. Recursive object/JSON/byte privacy tests cover original
  content, queries, identities, credentials, and keys; the real subprocess test
  requires empty stderr. Contracts and serializer
  (src/conversational_memory/entrypoints/evaluation.py:389), real CLI privacy
  test (tests/evaluation/test_evaluation_cli.py:177).

- Truthful verdicts and exits: hard failures take precedence over quality
  PARTIALs. CLI exit 0 means completed evaluation with no FAIL; exit 1 covers
  hard evaluation failures; exit 2 covers prerequisites preventing completion.
  Failure reports preserve recorded observations without fabricating future
  retrieval evidence. Unsupported arguments and ordinary exception diagnostics
  are redacted. Classification
  (src/conversational_memory/entrypoints/evaluation.py:1051), CLI boundary
  (src/conversational_memory/entrypoints/cli.py:1575), exit/failure tests
  (tests/evaluation/test_evaluation_cli.py:63).

The recorded real results remain:

| Case | Verdict | Primary evidence |
| --- | --- | --- |
| 1 | PARTIAL | Expected current ID omitted; no_relevant_memory, 0 tokens |
| 2 | PARTIAL | Expected current ID omitted; no_relevant_memory, 0 tokens |
| 3 | PARTIAL | Expected current ID omitted; no_relevant_memory, 0 tokens |
| 4 | PASS | m10-case4-c0000 selected; 20/128 tokens |
| 5 | PASS | no_relevant_memory, 0 tokens |
| 6 | PASS | no_relevant_memory, 0 tokens with 17 owned admissions |

Cases 1–3 explicitly report expected_memory_not_selected; their expected IDs
appear as below-threshold exclusions. Their false quality checks remain visible.
Totals are 3 PASS, 3 PARTIAL, 0 FAIL, without an aggregate success score.

The hard safety evidence is sufficient for the approved scope:

- Primary and repeat checks cover ownership, indexed/current eligibility,
  tombstones, stale targets, sensitive absence, forbidden apartment selection,
  relevance, complete blocks, exact tokens, budget bounds, and result consistency.
  Persisted bidirectional supersession chains and case4’s two owners are
  inspected. Primary assertions (tests/evaluation/test_primary_execution.py:90).

- Sensitive case5 admission rejects before embedding, ID allocation, or
  persistence. Its rejected-target forget action returns opaque not_found
  without clock/vector work or SQLite mutation. Accounting preserves the
  distinction between baseline 17 candidates and M10’s 16 admissions plus one
  control action.

- Admitted case5 filler forgetting proves exact deletion/completion times,
  complete cleanup, mapping removal, and absence after restart through CURRENT
  and HISTORICAL retrieval. Existing regressions additionally prove that
  forgetting a replacement does not reactivate its superseded predecessor.
  Supplemental proof (tests/evaluation/test_supplemental_execution.py:88),
  nonreactivation regression (tests/regression/test_forgetting.py:106).

- Fresh composition invokes normal startup recovery before primary and repeat
  retrieval. Five-table persisted snapshots remain unchanged; repeatability
  compares outcome, ordered IDs, ordered exclusions, tokens, and exact context.
  Zero-budget probes return empty context/IDs and zero tokens: case4 truthfully
  reports budget_excluded; the other cases retain no_relevant_memory.
  Restart/zero-budget assertions
  (tests/evaluation/test_supplemental_execution.py:56).

Recorded maker verification (.genesis/checkpoints/M10.md:386) documents 179
locked, 4 architecture, and 829 full offline tests passing without skips, real
CLI exit 0, and passing Ruff, explicit-source mypy, pip check, help,
input/tokenizer hashes, graph validation, and diff checks. These are historical
execution results reviewed here, not rerun results.

Every changed path complies with the M10 freeze decision
(.genesis/decisions/M10-freeze-boundary-exceptions.md). Service/domain/adapters,
dependencies, migrations, original inputs, architecture tests, PLAN/DONE,
graph, implementation notes, and wiki remain frozen. No M11 or production
behavior entered scope. CLI output claims only a pre-production reference
implementation. Existing installation-isolation history, cache-provenance/location
limitations, unavailable review skill, and process-only locking remain unresolved
and preserved.

M10 remains open pending the human quiz and explicit closure authorization.
This review starts neither.

Reviewer-supplied `git status --short`:

```text
 M .genesis/checkpoints/CURRENT.md
 M .genesis/decisions/decisions-manifest.md
 M src/conversational_memory/entrypoints/cli.py
?? .genesis/checkpoints/M10.md
?? .genesis/decisions/M10-fixed-workload-evaluation-bindings.md
?? .genesis/decisions/M10-freeze-boundary-exceptions.md
?? src/conversational_memory/entrypoints/evaluation.py
?? tests/evaluation/
```

### Recording boundary and remaining gates

- Full report recorded without erasing earlier iteration/verification evidence,
  PARTIAL limitations, environmental limitations, or historical incidents.
- Only M10.md and CURRENT.md changed in this recording task. No tests/model
  execution or other maker verification reruns; no source, tests, decisions,
  PLAN, DONE, context graph, or other artifacts changed.
- Human quiz not begun; closure not activated. Stop with M10 open pending
  human quiz and explicit closure authorization. No staging, commit, or push.

## M10 human quiz · 2026-09-18

- Canonical procedure: three targeted questions asked one at a time, waiting
  for the human's own answer and assessing it before the next question.
  Actual answers below are recorded from the conversation, not invented.
- Q1 (design): Why does M10 keep the six original workloads and retrieval
  settings frozen rather than tuning them until every case reports PASS?
  - Human answer: "It keeps the workloads and settings fixed so the evaluation
    stays fair and reproducible. Changing them after seeing results could hide
    real retrieval limitations. The PARTIAL cases are useful evidence of where
    the current system needs improvement, while safety failures would still
    count as FAIL."
  - Assessment: PASS — understands reproducibility, evidence integrity, truthful
    quality limitations, and the separate hard-failure boundary.
- Q2 (safety): Cases 1–3 are PARTIAL because expected useful memories were
  omitted. What would make a case FAIL instead, and can PASS results in other
  cases compensate for it?
  - Human answer: "A case becomes FAIL if it breaks a hard safety or state
    rule—for example, another user’s memory appears, sensitive or deleted
    content is exposed, stale/superseded memory is used as current, or the token
    budget is violated. No. PASS results in other cases cannot compensate for a
    FAIL."
  - Assessment: PASS — correctly identifies hard safety/state violations and
    rejects compensating aggregates; omission-only PARTIALs are not safety FAILs.
- Q3 (outcomes): The real evaluation reports 3 PASS, 3 PARTIAL, and 0 FAIL,
  with CLI exit 0. What does exit 0 establish, and what does it not establish
  about retrieval quality or production readiness?
  - Human answer: "Exit 0 shows that the evaluation completed with no hard
    safety or state failures. It does not mean retrieval quality is perfect,
    because three cases are still PARTIAL. It also does not mean the system is
    production-ready; it is only a pre-production reference implementation."
  - Assessment: PASS — correctly understands completed no-FAIL evaluation,
    visible retrieval-quality limitations, and the pre-production claim boundary.
- Outcome: PASS 3/3. Independent L4 APPROVE remains unchanged. Cases1-3
  remain PARTIAL/expected_memory_not_selected, cases4-6 PASS, totals 3 PASS,
  3 PARTIAL, 0 FAIL; no tuning or reinterpretation of the executed results.
- All earlier review/verification evidence and historical incidents preserved.
  Installation-isolation history, model-cache provenance, Windows-temporary
  tokenizer location, unavailable review skill, and process-only locking remain
  unresolved; quiz completion does not resolve or waive these limitations.
- Scope: agentic-swe-master/canonical quiz evidence bookkeeping only, in
  M10.md and CURRENT.md. No verification reruns, implementation/test/governance
  changes, closure activation, staging, commit, or push. M10 remains open pending
  explicit closure authorization; no post-M10 work.

## Authorized formal M10 closure · 2026-09-18

- Current status: CLOSED — M10 complete at 2026-09-18T12:20:24Z. Earlier open,
  pending-review, and pending-quiz entries remain historical evidence, not
  current state. Approved M1-M10 roadmap complete; no next milestone authorized.
- Authority: explicit user authorization for formal canonical closure, only the
  six lifecycle records below, fresh required closure verification, and no
  implementation/input/decision changes, staging, commits, pushes, or next work.
- Human gates: fresh independent L4 APPROVE and the actual canonical human quiz
  PASS 3/3, both recorded above and preserved. No new review or invented answers.
- Skills: agentic-swe-master, modular-architecture, production-readiness,
  llmops-ai-agents for lifecycle/evaluation verification and record synchronization.
  No new design, policy, behavior, or production-readiness claim.
- Completion scope: strict original six-file loader and fixture/baseline SHA
  validation; immutable closed evaluation contracts and deterministic privacy-safe
  serialization; real typed fixture adaptation and sequential case-local durable
  SQLite/FAISS execution through normal recovered composition; primary CURRENT,
  restart/persistence/repeatability, exact zero-budget, sensitive-rejection and
  forgetting probes; hard safety assertions and truthful case-specific quality
  verdicts; evaluate-fixed-workload CLI with documented 0/1/2 exit policy.
  Existing M1-M9 memory behavior is unchanged.
- All model-dependent closure commands used this process-local prefix, with the
  existing .venv in /home/shubham/projects/conversational-memory-intelligence:

  ```bash
  env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES= CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  ```

- Locked: `.venv/bin/python -m pytest tests/evaluation tests/regression tests/architecture -q`
  — 179 passed in 81.19s, numeric exit 0; no skips or deselections.
- Architecture: `.venv/bin/python -m pytest tests/architecture -q`
  — 4 passed in 0.20s, numeric exit 0.
- Full offline: `.venv/bin/python -m pytest tests -q`
  — 829 passed in 101.69s, numeric exit 0; no skips or deselections, including
  real-model and fixed-workload tests. No test-selection markers excluded gates.
- Real CLI: `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload`
  — numeric exit 0; one deterministic privacy-safe JSON report with the explicit
  pre-production reference implementation notice and no diagnostic chatter.

  | Case | Verdict | Reason | Primary outcome | Selected ID | Tokens / budget |
  | --- | --- | --- | --- | --- | --- |
  | case1 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case2 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case3 | PARTIAL | expected_memory_not_selected | no_relevant_memory | none | 0 / 128 |
  | case4 | PASS | expected_behavior_confirmed | memories_selected | m10-case4-c0000 | 20 / 128 |
  | case5 | PASS | expected_behavior_confirmed | no_relevant_memory | none | 0 / 128 |
  | case6 | PASS | expected_behavior_confirmed | no_relevant_memory | none | 0 / 128 |

- Totals: 3 PASS, 3 PARTIAL, 0 FAIL. Every case executed, repeatable, and
  restart_verified. All applicable hard safety/state, input preservation,
  privacy, exact-token, owner/lifecycle/relevance, zero-budget, and forgetting
  checks true. Only cases1-3 expected_primary_ids quality checks are false;
  their required useful IDs remain below-threshold exclusions. Case4 zero-budget
  is budget_excluded; others truthfully retain no_relevant_memory. No tuning,
  altered expectation, weighted compensation, hidden omission, or failure waiver.
- Exit 0 is correct under the binding: completed evaluation without FAIL.
  It does not establish perfect recall or production readiness. Completion
  accepts reproducible truthful PARTIAL evidence, not universal PASS quality.
- Frozen report settings: real CPU all-mpnet-base-v2 at immutable revision
  e8c3b32edf5434bc2275fc9bab85f82640a19130, dimension 768, threshold 0.50,
  limit 10, primary budget 128, cl100k_base, primary frozen_time
  2026-01-02T00:00:00Z, seed 10. No retrieval-setting changes or fallbacks.
- `.venv/bin/python -m ruff check src tests` — All checks passed!, numeric exit 0.
- `.venv/bin/python -m mypy src/conversational_memory` — no issues in 32 source
  files, numeric exit 0.
- `.venv/bin/python -m pip check` — No broken requirements found, numeric exit 0.
  Known non-failing warning: /home/shubham/.cache/pip or its parent was not owned
  or writable; pip disabled that cache. No installs, permission/cache repair,
  downloads, or persistent configuration changes.
- `.venv/bin/python -m conversational_memory.entrypoints.cli --help` — numeric
  exit 0; all prior demos and evaluate-fixed-workload present.
- `.venv/bin/python -m conversational_memory.entrypoints.cli evaluate-fixed-workload --help`
  — numeric exit 0; explicit offline/pre-production and exit 0/1/2 policy.
- Fixture/baseline/tokenizer checks before and after real CLI: read-only
  `.venv/bin/python -B -c` using hashlib.sha256 over file bytes and re.findall
  of the eight approved binding values; asserted exact equality/order and eight
  files/values. Tokenizer expected hash independently extracted from
  inspect.getsource(tiktoken_ext.openai_public.cl100k_base). Both runs exited 0.
  Actual approved input hashes:

  ```text
  case1_irrelevant_contradictory.json c674bdb684b1f6699954fb984f42d106b8c2c5c2be30a81c07c5463ac70a3b98
  case2_preference_change.json 1ae1b665c66381d7c49e3a8bf2004a4bf6b86be3fdb0d4f17aec3b9783ddae2d
  case3_long_context.json 7d1a5beb79f7f8e7aa44a9376a016dc0e1bf00778fbace70270498977e427762
  case4_multi_user.json 51e2f14e49917da2dffd3afe835d4bedfed5178e3820fee108cc2d241aa37917
  case5_sensitive_memory.json 24d03d0811f68c53221be7084ae6b6f77d2375cf79402538d807d45d62b6b768
  case6_cold_start.json 2dac46c31aadef03800903b328151aabc0193ff3be7245e2c165bd5863560a48
  experiments/baseline_protocol.md 8731f90878db8aa656f3cc872b9856fd3c63661cc6d638c71cfe46c6094cc6e3
  experiments/baseline_results.csv 8fff27539660715a09b9f83f9e61f1a0bc9405630a1c708b8f854f5a9cb857f5
  ```

- Existing tokenizer file:
  /mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache/9b5ad71b2ce5302211f9c61530b329a4922fc6a4.
  Expected SHA-256:
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
  Actual SHA-256:
  223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7.
  No Windows checkout/file edits or tokenizer download.
- Context graph: pre-close structural validation exit 0 (36 unique nodes,
  43 unique edges, resolved endpoints, nine nonempty invariants, Kahn traversal
  visits every node). Metadata refreshed and the M10 truthful/safe fixed-
  workload invariant added. Final validation checks the same 36-node/43-edge
  topology and all ten nonempty invariants; original nine preserved exactly.
  Structural validation is not a substitute for behavioral safety tests.
- Runtime read-only version evidence: Python 3.12.3, pip 26.2.1,
  torch 2.14.0+cpu, sentence-transformers 5.7.0, faiss-cpu 1.15.0,
  tiktoken 0.14.0, pydantic 2.13.5, pytest 9.1.1, Ruff 0.16.5, mypy 1.20.2.
- Preservation audit: before editing closure records, all 194 protected tracked/
  untracked source/test/decision/artifact file hashes match the pre-verification
  snapshot; comparison repeated after closure edits. Ignored generated test/type/
  bytecode caches are not claimed immutable. No working-.venv byte-for-byte
  preservation/restoration claim is made for the historical isolation incident.
- `git diff --check` — numeric exit 0 before updates; final check after updates.
- G1/G2: canonical lifecycle followed and six required records synchronized.
  G3: actual token-usage counter unavailable; no fabricated count.
  G4: fresh closure gates passed. G5: existing independent L4 APPROVE retained;
  human quiz PASS 3/3 preserved. Optional explain-diff artifact not generated.
- All prior maker/reviewer distinctions, original verification logs, quiz Q&A,
  PARTIAL limitations, and historical incidents retained. Unreconstructed model-
  cache provenance, Windows-temporary tokenizer location, unavailable review
  skill, installation-isolation/restoration uncertainty and unrestored pip-cache
  changes, process-only locking, and current pip-cache warning remain unresolved.
- Exact task-only files: .genesis/checkpoints/M10.md,
  .genesis/checkpoints/CURRENT.md, .genesis/PLAN.md, .genesis/DONE.html,
  .genesis/implementation-notes.html, .genesis/context-graph.json.
- Final state: M10 CLOSED; M1-M10 complete within the approved pre-production
  reference-implementation scope. No next milestone or post-M10 work authorized
  or begun; no source/tests/decisions/workload/baseline changes, installation,
  staging, commit, or push.
- Final post-synchronization checks: context JSON/unique nodes/unique edges/
  endpoint/invariant/Kahn acyclic validation exited 0: 36 nodes, 43 edges,
  10 nonempty invariant records. Separate in-memory comparison confirms the
  original topology, nine invariants, project, and freeze metadata unchanged;
  only generated timestamp and the approved-scope M10 invariant added.
  All 194 protected tracked/untracked file hashes still match exactly.
  `git diff --check` exited 0 with no output after closure updates.
