# M7 checkpoint

## G0 preflight · 2026-09-08

- State: `PARTIAL`
- BUILD: not started
- Previous milestone: M6 closed after independent L4 APPROVE, canonical quiz
  PASS 3/3, and authorized closure verification.
- Evidence: canonical Genesis records; M1 locking/FAISS contracts; M2/M5
  tombstone rules; M4 no-reactivation; M6 trusted clock; ADR-006/007; API, data
  model, threat model, D4 forgetting/adversarial/no-reactivation experiments;
  relevant wiki rules; and current contracts, ports, SQLite, FAISS, CLI/tests.
- Existing foundation: read-side tombstones, authoritative owner-scoped SQLite,
  stable mappings, durable copy-on-write FAISS, process-wide serialization, and
  preserved lifecycle/supersession relationships.
- Missing behavior: typed forget contracts; opaque lookup; atomic tombstone and
  cleanup state; nullable physical identity; targeted removal; acknowledgement;
  retry/restart/concurrency handling; regressions; and `demo-forgetting`.
- Binding: `M7-forgetting-bindings.md` defines the complete policy. In
  particular, an owned record without a mapping becomes tombstoned null-ID
  cleanup-pending; M7 retries use zero clocks and zero FAISS and never complete
  it, while M8 owns recovery.
- Timestamp invariant: pending cleanup has null `completed_at`; known-ID
  completion stores a separate validated trusted UTC completion time atomically.
- Boundary: `M7-freeze-boundary-exceptions.md` authorizes exactly five Genesis
  governance paths. No M8-M10 behavior is authorized.
- Gate: G0 is complete with verdict `PARTIAL`; L1 BUILD remains unstarted until
  separately authorized.

## Iteration 1 · L1 BUILD · strict forgetting contracts · 2026-09-08

- Authorization: apply the approved M7 governance records and implement only
  the minimal domain/application contract slice for strict `ForgetRequest` and
  `ForgetResult` behavior.
- Boundary: added `domain/forgetting.py`, amended application/domain exports and
  `application/contracts.py`, and added `tests/unit/domain/test_forgetting.py`.
  No service, port, SQLite, migration, FAISS, retry, CLI, demo, dependency, or
  M8-M10 behavior was added.
- Regression-first proof: the new focused test exited 2 during collection with
  `ImportError: cannot import name 'ForgetRequest'` before implementation.
- Implementation: added the exact three-value `ForgetOutcome`; a strict,
  owner-free, one-field request that preserves an opaque ID and rejects empty,
  coerced, or whitespace-surrounded values; and a strict result whose validated
  shape enforces opaque `not_found`, completed forgetting, and retryable cleanup
  pending semantics with aware-UTC deletion timestamps.
- Focused forgetting result: 25 passed in 0.27s.
- Admission/forgetting compatibility result: 50 passed in 0.32s.
- Changed-file Ruff: passed after replacing one lint-only naive-datetime test
  constructor with an equivalent `.replace(tzinfo=None)` fixture.
- Changed-source mypy: passed with no issues in 2 files.
- `git diff --check`: passed.
- State: iteration complete; later persistence, FAISS, retry, and demo work has
  not started and requires separate authorization.

## Iteration 2 · L1 BUILD · SQLite forgetting persistence · 2026-09-08

- Authorization: implement only the ordered SQLite migration and repository
  operations for owner-scoped logical tombstoning and durable cleanup state,
  with real SQLite regressions. Service orchestration, FAISS removal, broader
  retries, CLI/demo, and M8-M10 behavior remained excluded.
- Regression-first proof: `tests/integration/test_forgetting.py` initially
  exited 2 during collection because `ForgettingCleanupState` did not exist.
- Migration: added ordered `0003_add_memory_forgetting.sql` with nullable
  `vector_id`, `cleanup_pending|complete`, trusted `requested_at`, nullable
  `completed_at`, exact pending/complete checks, unique known vector IDs,
  composite owner/memory foreign key, and tombstone backfill as pending only.
- Port/persistence contracts: added immutable `ForgettingRecord` and
  `ForgettingTarget` records with stable-ID, owner, aware-UTC timestamp, and
  cleanup-state invariants; added owner-scoped target read and atomic initiation
  operations to the application repository port.
- SQLite behavior: owner-scoped reads hide cross-owner records; initiation
  revalidates ownership in `BEGIN IMMEDIATE`, preserves an existing tombstone,
  atomically writes tombstone plus pending cleanup state, copies a mapping when
  present, stores null when absent, retains mappings, and returns stored state
  unchanged on repeat/restart.
- Failure evidence: a real SQLite abort trigger on cleanup-record insertion
  rolled back the new tombstone and cleanup row together while preserving the
  vector mapping.
- Null-ID evidence: an owned record with its mapping absent became tombstoned
  `cleanup_pending` with `vector_id=None` and `completed_at=None`; this
  persistence-only slice has no FAISS dependency or call path.
- Focused SQLite result: 6 passed in 5.57s.
- Combined M7 contract/persistence result: 31 passed in 5.31s.
- Changed-file Ruff: passed.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: iteration complete; MemoryService, physical FAISS removal,
  completion acknowledgement/retry orchestration, and demo work have not
  started and require separate authorization.

## Iteration 3 · L1 BUILD · FAISS cleanup and SQLite acknowledgement · 2026-09-08

- Authorization: add only targeted durable FAISS removal and known-vector
  SQLite completion acknowledgement with focused adapter/integration evidence;
  MemoryService/public dispatch, retry orchestration, CLI/demo, recovery, and
  M8-M10 behavior remained excluded.
- Regression-first proof: 5 of 11 focused tests failed before implementation:
  three on missing `FaissVectorIndex.remove` and two on missing
  `acknowledge_forgetting_complete`.
- FAISS behavior: removal validates the stable ID, reloads and verifies the
  latest durable generation, clones it, removes exactly one present ID,
  verifies candidate and persisted absence, and publishes through the existing
  copy-on-write protocol. An already-absent ID refreshes current state and
  returns without publishing.
- Preservation/failure evidence: restart inspection proved the removed ID is
  absent and an unrelated ID remains; forced publication failure preserved the
  original durable generation and both vectors.
- SQLite acknowledgement: the owner-scoped atomic operation requires a
  tombstoned known-ID pending record, rejects stored/live identity mismatch,
  stores the supplied validated UTC `completed_at`, marks cleanup complete, and
  removes only the matching live mapping in one transaction. Exact completed
  state is idempotently readable.
- Cross-adapter evidence: tests sequence verified durable FAISS absence before
  acknowledgement. Forced FAISS publication failure left the SQLite tombstone,
  pending record, null completion time, and mapping intact. A forced SQLite
  acknowledgement failure likewise rolled back completion and retained the
  pending mapping.
- Focused adapter/integration result: 12 passed in 5.78s.
- Combined M7 contract/adapter/persistence result: 37 passed in 5.81s.
- Changed-file Ruff: passed.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: iteration complete; service dispatch, public orchestration, complete
  retry/restart behavior, and demo work have not started and require separate
  authorization.

## Iteration 4 · L1 BUILD · MemoryService forgetting orchestration · 2026-09-08

- Authorization: wire only `MemoryService.forget` through the existing process
  write lock and add real integration regressions for logical exclusion,
  privacy, idempotency, failures, and no reactivation. CLI/demo, recovery, and
  M8-M10 behavior remained excluded.
- Regression-first proof: after correcting the test import to use the existing
  domain outcome export, 6 service scenarios failed while 9 persistence tests
  passed; every failure was the missing `MemoryService.forget` method.
- Orchestration: validates trusted context and the strict request, resolves the
  target within the lock, returns identical opaque `not_found` without clock or
  FAISS work, reuses durable cleanup state, initiates the atomic tombstone,
  returns null-ID cleanup pending with zero FAISS calls, removes known IDs, and
  acknowledges completion with a separately validated trusted UTC time.
- Failure semantics: FAISS removal and SQLite acknowledgement failures return
  retryable `physical_cleanup_pending` after the tombstone; invalid clock output
  remains fail-closed; completed state returns `already_forgotten` without new
  clock, FAISS, or SQLite mutation.
- Retrieval evidence: a forgotten indexed memory was immediately absent from
  both current and explicit historical retrieval. Cross-owner forgetting made
  no clock, FAISS, tombstone, or cleanup-state change.
- Retry evidence: null-ID repeat used one initial clock total and zero FAISS;
  acknowledgement failure retained durable pending state, then an identical
  retry observed the vector already absent and completed without duplicate
  vector publication.
- Relationship evidence: forgetting the active replacement preserved both
  stored relationship directions and left its predecessor superseded; current
  retrieval returned no reactivated predecessor.
- Focused M7 result: 43 passed in 5.96s.
- Changed-file Ruff: passed.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: orchestration iteration complete; CLI/demo and later recovery work have
  not started and require separate authorization.

## Iteration 5 · L1 BUILD · durable forgetting CLI demo · 2026-09-09

- Authorization: add only `demo-forgetting`, parser coverage, and the matching
  integration regression using the real durable SQLite/FAISS composition.
  General recovery, schedulers, observability, and M8-M10 behavior remained
  excluded.
- Regression-first proof: the focused integration test exited 2 during
  collection with `ImportError: cannot import name '_demo_forgetting'` before
  the CLI implementation existed.
- Demo behavior: admitted an original, its explicit replacement, and an
  unrelated memory; an opaque cross-owner forget returned `not_found`, while
  the owner-authorized operation returned `forgotten` and immediately excluded
  the replacement from current and explicit historical retrieval.
- Durable proof: a freshly composed service reopened the same SQLite database
  and FAISS directory and continued to exclude the forgotten replacement. The
  persisted original remained `superseded`, its `superseded_by` link still
  named the tombstoned replacement, the replacement retained its forward link,
  and the predecessor was not reactivated.
- Physical proof: output derived from persisted mappings, forgetting state,
  FAISS generation metadata, and post-restart retrieval showed vector ID 2
  absent, unrelated vector ID 3 retained and retrievable, and vector count
  reduced from 3 to 2. Persisted cleanup was `complete` with non-null requested,
  completed, and deletion timestamps.
- Focused M7 result: 45 passed in 9.94s.
- Offline CPU-only `demo-forgetting`: exit 0 using the recorded immutable model
  and tokenizer caches; all authorization, immediate exclusion, restart,
  relationship, targeted-cleanup, and persisted-state assertions passed.
- Changed-file Ruff: passed.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: bounded CLI/demo iteration complete; M7 remains open and M8 recovery
  has not started.

## Maker verification · BLOCKED · 2026-09-09

- Exact locked M7 command:
  `.venv/bin/python -m pytest tests/unit/domain tests/adapter
  tests/integration/test_forgetting.py tests/regression/test_forgetting.py -q`.
  Result: exit 4 before collection because the locked path
  `tests/regression/test_forgetting.py` does not exist.
- Architecture suite: 4 passed in 0.24s.
- Full offline suite used `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, the recorded immutable Hugging Face cache, and the
  recorded `TIKTOKEN_CACHE_DIR`. Result: 367 passed, 1 failed in 20.93s. The
  sole failure was
  `tests/integration/test_current_state_filtering.py::test_seeded_ineligible_states_never_enter_current_state_results`:
  its completed-M2 assertion expects schema migration versions `[1, 2]`, while
  the approved M7 migration produces `[1, 2, 3]`.
- Real offline CPU-only `demo-forgetting`: exit 0. It proved opaque cross-owner
  denial, authorized forgetting, immediate current/historical exclusion,
  durable restart exclusion, preserved supersession with no predecessor
  reactivation, removal of only vector ID 2, preservation and retrieval of
  unrelated vector ID 3, vector-count change 3 to 2, and persisted `complete`
  cleanup with requested/deleted/completed timestamps.
- Ruff: passed for `src` and `tests`.
- Explicit-source mypy: passed with no issues in 27 source files.
- `pip check`: passed with no broken requirements; pip also warned that its
  unwritable default cache was disabled.
- CLI help: exit 0 and listed `demo-forgetting`.
- Context graph: `python -m json.tool` passed; endpoint and cycle validation
  reported 36 nodes, 43 edges, seven invariants, valid endpoints, and an
  acyclic graph. An initial ad hoc validator invocation had quoting syntax
  error; the corrected validator passed.
- `git diff --check`: exit 0.
- Verdict: maker verification is blocked by the missing locked regression path
  and stale completed-M2 migration assertion. No source or test repair was
  authorized or made. M7 remains open; L4 and M8 have not started.

## Maker verification repair and rerun · PASS · 2026-09-09

- Authorization: fix only the two recorded verification blockers and the
  package marker required for literal locked-suite collection. No production
  code or behavior changed.
- Locked regression entry: added
  `tests/regression/test_forgetting.py` with durable SQLite/FAISS evidence that
  owner-authorized forgetting survives restart, excludes the target from
  current and historical retrieval, preserves the predecessor's superseded
  state and both relationship directions, removes the target mapping/vector,
  and retains unrelated mappings/vectors.
- Completed-M2 maintenance: changed only the migration-list assertion in
  `tests/integration/test_current_state_filtering.py` from `[1, 2]` to
  `[1, 2, 3]`; its current-state assertions are unchanged.
- Collection repair: added empty `tests/regression/__init__.py` so the literal
  locked command does not collide with the unit module of the same basename.
  Both exact paths are registered in the M7 freeze-boundary decision.
- Literal locked M7 command: 191 passed in 14.88s, exit 0.
- Full offline suite: 369 passed in 23.09s, exit 0, using the recorded offline
  CPU-only Hugging Face and tokenizer cache environment.
- Ruff: passed for `src` and `tests`.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: exit 0.
- Verdict: M7 maker verification passes after the bounded test-only repairs.
  M7 remains open; independent L4 and M8 have not started.

## L4 block remediation · L2 DEBUG · 2026-09-09

- Scope: fixed only the three reported M7 evidence blockers. No reconciliation,
  rebuild, scheduler, observability, M8-M10 behavior, or unrelated production
  behavior was added.
- Durable-removal correction: the M7 FAISS `remove` path now requires both
  durable generation files and their existing verification to succeed. Missing
  files fail as missing and a partial pair fails as incomplete. Cached
  in-memory absence can no longer authorize an already-absent success or SQLite
  cleanup acknowledgement.
- Uncertain-durability evidence: real adapter tests remove both final files and
  remove one member of the final pair. Both fail closed. A real service/SQLite
  regression with a partial FAISS generation returns retryable cleanup pending,
  retains the live mapping, and leaves `completed_at` null.
- Binding evidence completed: regressions cover null-ID retry after restart and
  later mapping appearance with zero clock/FAISS work; mapped and unmapped
  tombstone migration backfill; invalid deletion and completion clocks; stored
  versus live vector-ID mismatch before FAISS; unavailable owner lookup and
  subsequent lock release; every active/superseded/expired state crossed with
  every pending/indexed/failed state; and concurrent identical workflows
  producing one cleanup record and one targeted removal.
- Boundary: registered exactly
  `src/conversational_memory/infrastructure/faiss_index.py` for the M7
  targeted-removal correction. Existing test and governance boundaries remain
  unchanged.
- Focused M7 files: 65 passed in 12.37s before the final minimality cleanup.
- Literal locked M7 command: 210 passed in 11.47s after the final minimality
  cleanup.
- Full offline suite: 388 passed in 23.35s using the recorded CPU-only model and
  tokenizer cache environment.
- Ruff: passed for `src` and `tests`.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: the reported blockers are remediated with maker evidence. M7 remains
  open; no fresh L4 review or M8 work has started.

## L4 idempotency/privacy remediation · L2 DEBUG · 2026-09-09

- Scope: fixed only retained admission idempotency after completed forgetting
  and the missing nonexistent-ID privacy regression. No recovery,
  reconciliation, M8-M10 behavior, or unrelated production change was added.
- Regression-first evidence: the two new focused scenarios initially produced
  one pass and one failure. Nonexistent forgetting already matched cross-owner
  behavior; completed-forgetting admission replay incorrectly entered the new
  admission path and attempted a second memory-ID allocation.
- Cause and correction: SQLite idempotency lookup required an inner join to the
  live vector mapping, but successful forgetting intentionally removes that
  mapping while retaining admission idempotency, memory, and embedding rows.
  The lookup now left-joins the mapping and returns `indexing_work=None` only
  when it is absent. The retained indexed admission result therefore replays
  through the established indexed-result path without embedding, ID
  allocation, persistence, vector publication, mapping restoration, or
  tombstone mutation.
- Real replay proof: after admission and completed forgetting, the identical
  original request returned the exact original `AdmissionResult`; embedder,
  memory-ID allocator, FAISS add, and FAISS remove counts remained 1; one memory
  and one idempotency row remained; the tombstone and complete cleanup remained;
  and no live vector mapping was recreated.
- Opaque nonexistent proof: a nonexistent ID returned the exact same
  `not_found` result as a cross-owner ID with zero clock reads, zero FAISS
  removals, and byte-equivalent queried rows across memories, idempotency,
  embeddings, mappings, and forgetting state.
- New focused scenarios: 2 passed in 9.26s.
- Focused M7 files: 67 passed in 12.56s.
- Literal locked M7 command: 212 passed in 13.05s.
- Full offline suite: 390 passed in 28.19s, numeric exit 0, using the recorded
  CPU-only model and tokenizer cache environment.
- Ruff: passed for `src` and `tests`.
- Explicit-source mypy: passed with no issues in 27 source files.
- `git diff --check`: passed.
- State: both reported findings are remediated with maker evidence. M7 remains
  open; no fresh L4 review or M8 work has started.

## Fresh independent L4 review · APPROVE · 2026-09-09

- Verdict: `APPROVE`; no blocking findings.
- Independence: the reviewer inspected the complete tracked and untracked M7
  diff without editing files or rerunning tests.
- Admission idempotency: the reviewer confirmed that completed forgetting no
  longer hides the retained idempotency record when its live mapping is
  removed, and that indexed replay returns before any new indexing work. The
  real regression proves the exact original result, unchanged embedding/ID/add/
  remove counts, retained tombstone and cleanup state, and no recreated mapping.
- Privacy: the reviewer confirmed nonexistent-ID forgetting returns before
  clock access or mutation and exactly matches cross-owner `not_found`. The
  regression proves zero clock/FAISS work and unchanged rows across all five
  persistence tables.
- Remaining invariants: strict contracts, migration/backfill, atomic tombstone
  and acknowledgement, separate trusted timestamps, complete lifecycle/indexing
  coverage, null-ID restart behavior, mismatch rejection, failure handling,
  serialization, global retrieval exclusion, durable FAISS removal, restart,
  and no reactivation were inspected and accepted.
- Boundary: the complete M7 diff conforms to the approved binding and updated
  freeze exceptions; no M8-M10 implementation was found.
- Accepted maker evidence: 67 focused tests, 212 locked-gate tests, 390 full
  offline tests, Ruff, mypy, diff integrity, and the earlier successful
  persisted-state `demo-forgetting` evidence.
- State: M7 remains open pending the canonical three-question human quiz and
  separate explicit closure authorization. M8 has not started.

## Canonical post-APPROVE human quiz · PASS · 2026-09-09

- Question 1 · ordering/failure semantics: explained that SQLite writes the
  tombstone first for immediate exclusion and that FAISS failure leaves the
  memory hidden with cleanup pending and no completion acknowledgement.
  Assessment: pass.
- Question 2 · missing physical identity: explained that an untrusted or absent
  vector ID requires zero FAISS calls to prevent deletion of the wrong vector,
  remains cleanup pending even if a mapping later appears, and defers
  reconciliation to M8. Assessment: pass.
- Question 3 · change impact/idempotency: explained that identical original
  admission replay must return the original result after forgetting and must
  never create a memory/vector, restore a mapping, clear the tombstone, or
  alter completed cleanup state. Assessment: pass.
- Quiz result: `PASS 3/3`.
- State: independent L4 APPROVE and the canonical quiz are complete. M7 remains
  open pending explicit closure authorization; M8 has not started.

## Authorized milestone closure · 2026-09-09

- Authorization: the user explicitly authorized canonical M7 closure and
  prohibited source/test changes, staging, commits, pushes, or beginning M8.
- Required human gates: fresh independent L4 verdict `APPROVE`; canonical quiz
  `PASS 3/3`.
- Completion scope: strict owner-scoped forgetting; opaque missing/cross-owner
  results; tombstone-first global retrieval exclusion; durable nullable-ID
  cleanup state; targeted verified FAISS removal; atomic SQLite completion;
  restart-safe and concurrent idempotency; preserved admission replay; and
  preserved supersession relationships without reactivation. General recovery,
  reconciliation, schedulers, observability, and M8-M10 behavior remain
  excluded.
- Closure verification: literal locked M7 suite `212 passed in 10.17s`;
  architecture suite `4 passed in 0.13s`; complete offline suite `390 passed in
  17.19s`; Ruff passed; explicit-source mypy passed with 27 source files;
  `pip check` reported no broken requirements; CLI help exited 0 and listed all
  seven demos.
- Demo evidence: offline CPU-only `demo-forgetting` exited 0 and proved opaque
  cross-owner denial, owner-authorized completion, immediate current and
  historical exclusion, durable restart, retained bidirectional supersession
  without predecessor reactivation, targeted removal of vector ID 2,
  preservation of unrelated vector ID 3, and persisted complete cleanup with
  trusted deletion/request/completion timestamps.
- Context graph: JSON parsed; 36 nodes, 43 edges, and seven invariants had valid
  endpoints and remained acyclic.
- `git diff --check`: exit 0.
- Closure state: M7 is complete. M8 is the next planned milestone but is
  explicitly unstarted pending separate G0 authorization.
