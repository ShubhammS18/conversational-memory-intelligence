# M8 checkpoint

## G0 decision proposal · 2026-09-09

- State: `PARTIAL`
- BUILD: not started
- Previous milestone: M7 closed after fresh independent L4 APPROVE, canonical
  quiz PASS 3/3, and green closure verification.
- Objective: safely audit authoritative SQLite state and create, verify, or
  rebuild its derived FAISS generation without exposing an ineligible memory or
  duplicating durable identity.
- Evidence: canonical Genesis records; M1 SQLite/FAISS authority, stable-ID,
  publication, and process-lock contracts; M2/M5/M6 retrieval eligibility;
  M4 fail-closed supersession acknowledgement; M7 tombstone and cleanup
  handoff; ADR and design records; D4 recovery, migration, and scale evidence;
  relevant wiki rules; and current ports, SQLite migrations/repository, FAISS
  adapter, composition, CLI, and tests.
- Binding: `M8-recovery-and-reconciliation-bindings.md` defines exact authority,
  audit, rebuild set, readiness outcomes, mapping failures, orphan handling,
  pending/failed behavior, M7 cleanup handoff, empty-store behavior, verified
  publication, locking, and idempotency.
- Authority: SQLite is authoritative; FAISS is rebuilt derived state. Recovery
  never infers missing memory, owner, vector, or supersession identity.
- Readiness: `ready` requires a verified exact durable generation and no
  unresolved work; `degraded` permits only deterministically excluded
  unresolved records; every unsafe or ambiguous state is `unavailable`.
- Supersession: recovery may reconstruct a pending replacement’s stable vector
  but never promotes it or invents relationship acknowledgement. Ambiguous
  supersession recovery remains fail-closed.
- M7 handoff: known cleanup identities are excluded and completed only after
  verified durable absence; an audited later mapping may be adopted for a
  null-ID pending record; absent or conflicting identity remains degraded or
  unavailable exactly as bound.
- Boundary: `M8-freeze-boundary-exceptions.md` authorizes only the five M8
  governance files and the existing flat FAISS adapter/export mismatch. No
  migration or completed-milestone maintenance is pre-authorized.
- Exact locked test command:
  `.venv/bin/python -m pytest tests/adapter
  tests/integration/test_recovery.py
  tests/integration/test_startup_readiness.py tests/regression -q`.
- Required demo:
  `.venv/bin/python -m conversational_memory.entrypoints.cli demo-recovery`.
- Required full verification: architecture tests; complete offline CPU-only
  suite; Ruff; explicit-source mypy; pip check; CLI help; context-graph
  validation; `git diff --check`; and `git status --short`.
- G0 verdict: `PARTIAL`. The decisions were applied under explicit
  authorization; BUILD remained separately gated until iteration 1.
- Exclusions: M9 privacy-safe observability and M10 fixed-workload evaluation
  remain unstarted and unauthorized.

## Iteration 1 · L1 BUILD · SQLite recovery inventory · 2026-09-09

- Authorization: activate M8 and implement only the immutable recovery
  inventory/readiness contract and read-only SQLite recovery inventory.
- Regression-first proof: the focused test exited 2 during collection with
  `ModuleNotFoundError` for `conversational_memory.application.recovery` before
  implementation.
- Contract: added immutable `RecoveryReadiness`, `RecoveryVector`, and
  `RecoveryInventory` records and the application-owned repository operation.
- SQLite audit: reads one consistent transaction; verifies exact ordered
  migration identities, SQLite integrity and foreign keys; classifies stable
  mappings, stored float32 embeddings, indexing state, tombstones, and
  forgetting identity without mutating SQLite or FAISS.
- Classification evidence: covered valid indexed/pending/failed mappings;
  tombstone and cleanup exclusion; indexed versus safely excluded missing
  embeddings/mappings; duplicate and forgetting-ID mismatch; model/dimension
  mismatch; migration checksum failure; foreign-key integrity failure; and an
  empty valid inventory.
- Focused result: `8 passed in 8.36s`.
- Changed-scope Ruff: passed.
- Explicit-source mypy: passed with no issues in 28 source files.
- `git diff --check`: passed.
- State: iteration 1 complete. FAISS inspection/mutation, rebuilding,
  publication, composition, CLI/demo, migrations, and M9-M10 remain unstarted.

## Iteration 1 completion verification · 2026-09-09

- New recovery-inventory adapter tests: `8 passed in 13.55s`.
- Complete existing-plus-new adapter suite: `24 passed in 6.32s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 28 source files.
- `git diff --check`: passed.
- Verdict: iteration 1 verification is complete and green; iteration 2 has not
  started.

## Iteration 2 · L1 BUILD · exact FAISS recovery generation · 2026-09-09

- Authorization: implement only the FAISS adapter operation that derives and
  publishes an exact stable-ID generation from an audited recovery inventory.
- Regression-first proof: all six initial adapter scenarios failed because
  `FaissVectorIndex.reconcile_from_inventory` did not exist. A subsequent
  same-ID/stale-vector regression failed because ID equality alone incorrectly
  reused the generation before full vector comparison was added.
- Implementation: the recovery operation constructs a candidate solely from
  stored float32 inventory embeddings and stable IDs, verifies candidate and
  final durable pairs through the existing copy-on-write protocol, omits
  orphans, publishes a verified empty generation, and reuses an existing
  generation only when both its exact ID set and vectors match the inventory.
- Failure semantics: unavailable inventories, duplicate IDs, invalid vectors,
  candidate failures, publication failures, and final verification failures
  fail closed. No SQLite or forgetting state is mutated.
- Focused recovery/FAISS adapter result: `20 passed in 9.40s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 28 source files.
- `git diff --check`: passed.
- State: iteration 2 complete. Startup orchestration, SQLite mutation,
  forgetting acknowledgement, composition, CLI/demo, migrations, and M9-M10
  remain unstarted.

## Iteration 3 · L1 BUILD · application recovery coordinator · 2026-09-09

- Authorization: add only an application recovery coordinator using the
  existing process-wide write lock, SQLite inventory, and exact FAISS
  generation operation. M7 acknowledgement, startup composition, CLI/demo, and
  later milestones remained excluded.
- Regression-first proof: the new integration file exited 2 during collection
  because `RecoveryCoordinator` did not exist before implementation.
- Contracts: added immutable `RecoveryResult` with readiness, exact reason,
  publication status, final/orphan counts, and unresolved pending, failed, and
  cleanup counts; added application-owned inventory and generation protocols.
- Coordination: moved the existing process-wide lock to one application-owned
  module and reused that same lock for admission, forgetting, and recovery.
  Recovery obtains one inventory, refuses unsafe inventory before FAISS work,
  invokes exact-generation reconciliation only for safe inventory, and maps
  publication failure to unavailable readiness.
- Evidence: clean recovery rebuilt then reused the durable generation; pending
  and failed records produced degraded readiness with exact counts; unsafe
  inventory performed no FAISS creation; publication failure returned
  unavailable; and tombstoned cleanup-pending memory stayed omitted.
- Focused M8 result: `20 passed in 8.81s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: iteration 3 complete. M7 cleanup acknowledgement/null-ID adoption,
  startup composition, CLI/demo, migrations, and M9-M10 remain unstarted.

## Iteration 4 · L1 BUILD · M7 forgetting handoff · 2026-09-09

- Authorization: implement only known/null-ID M7 cleanup handoff after verified
  durable FAISS absence, using a separate trusted UTC completion time and the
  existing atomic acknowledgement.
- Regression-first proof: all nine recovery integration scenarios initially
  failed because the coordinator accepted no trusted clock and had no cleanup
  handoff behavior.
- Inventory: added immutable cleanup work containing the audited stored and live
  identities. Non-null conflicts remain unavailable. Completed tombstones are
  not counted as unresolved admission work.
- Known-ID behavior: exact-generation reconciliation omits the tombstoned ID;
  only after durable final verification does recovery read and validate one
  completion time and invoke the existing atomic acknowledgement.
- Null-ID behavior: without a live mapping, recovery remains degraded with zero
  clock and FAISS work. One exact later live mapping is adopted atomically only
  after its omission is durably verified, then acknowledged through the same
  known-ID path.
- Failure/idempotency evidence: acknowledgement failure retained tombstone,
  mapping, null completion time, and cleanup-pending state. Fresh restart
  completed known-ID work, and a repeat reused the exact generation without a
  new clock or acknowledgement.
- Focused recovery plus existing M7 forgetting result: `60 passed in 11.70s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: iteration 4 complete. Startup composition, CLI/demo, migrations, and
  M9-M10 remain unstarted.

## Iteration 5 · L1 BUILD · startup composition and readiness · 2026-09-09

- Authorization: wire the existing recovery coordinator into normal local
  composition before exposing `MemoryService`, without adding recovery policy,
  CLI/demo, migrations, or later-milestone behavior.
- Regression-first proof: the new startup-readiness integration file exited 2
  during collection because `compose_recovered_memory_service` did not exist.
- Composition: added an immutable local runtime containing the usable service
  and verified recovery result. Recovery runs before `FaissVectorIndex` and the
  service are exposed; only ready or degraded results proceed, while unavailable
  raises `ServiceUnavailableError` with the bound reason.
- Normal local path: `compose_local_memory_service` now uses the same recovery-
  aware composition on every fresh process/service construction. Schema setup
  failure is translated to unavailable startup rather than exposing a service.
- Evidence: ready and degraded startup returned usable services; unsafe SQLite
  state exposed none; missing durable FAISS state rebuilt before service
  creation; and a fresh second startup repeated the audit and reused byte-
  identical durable files without publication.
- Startup-readiness result: `5 passed in 8.35s`.
- Complete focused M8 result: `29 passed in 9.57s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: iteration 5 complete. CLI/demo, migrations, M9, and M10 remain
  unstarted.

## Iteration 6 · L1 BUILD · durable recovery CLI demo · 2026-09-09

- Authorization: add only `demo-recovery`, parser coverage, and structured
  output proving the existing M8 recovery policy with real durable SQLite and
  FAISS. No new policy, migration, observability, or later-milestone behavior
  was authorized.
- Regression-first proof: the two parser/output regressions initially failed
  during collection because `_demo_recovery` did not exist.
- Demo scenario: admitted an indexed memory and a second memory that was then
  durably forgotten; removed the indexed memory’s vector and inserted unmapped
  vector ID 999 to create a recoverable SQLite/FAISS mismatch.
- First recovery evidence: readiness `ready`, reason
  `ready_rebuilt_generation`, `rebuilt=true`, final vector count 1, and one
  orphan removed. The authoritative mapping retained stable vector ID 1;
  current retrieval selected only that memory, while the tombstoned memory
  remained absent from explicit historical retrieval.
- Restart evidence: a fresh repository, model adapter, tokenizer, recovery
  coordinator, FAISS adapter, and service reported `ready_existing_generation`,
  `rebuilt=false`, and byte-identical final index and metadata files.
- Real offline CPU-only `demo-recovery`: exit 0 using the recorded immutable
  Hugging Face and tiktoken caches.
- Focused M8 result: `31 passed in 6.52s`.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: iteration 6 complete. M8 remains open; maker verification and fresh
  independent L4 review have not started. M9-M10 remain unstarted.

## Maker verification · PASS · 2026-09-09

- Initial locked-suite attempt: 51 passed and 8 context-budget regressions
  failed because `TIKTOKEN_CACHE_DIR` was absent and tiktoken attempted a DNS-
  blocked fetch. No repository change was made.
- Cache prerequisite: verified
  `/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache/9b5ad71b2ce5302211f9c61530b329a4922fc6a4`
  exists. Its SHA-256 and the installed `cl100k_base` constructor’s expected
  SHA-256 were both
  `223921b76ee99bde995b7ff738513eef100fb51d18c93597a113bcffe865b2a7`.
- Literal locked M8 suite with that local cache setting: `59 passed in 6.57s`.
  No tokenizer download occurred.
- Architecture suite: `4 passed in 0.12s`.
- Complete offline CPU-only suite: `421 passed in 17.45s`, using
  `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, and the recorded immutable model and tokenizer caches.
- Real offline CPU-only `demo-recovery`: exit 0. It reported a ready rebuilt
  generation with stable vector ID 1, one orphan removed, the tombstoned memory
  absent, and a fresh ready second startup with no rebuild and byte-identical
  durable files.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `pip check`: no broken requirements; pip reported only that its unwritable
  default cache was disabled.
- CLI help: exit 0 and listed all eight demos including `demo-recovery`.
- Context graph: valid JSON; 36 nodes, 43 edges, seven invariants, valid edge
  endpoints, and no cycle.
- `git diff --check`: passed.
- Maker verdict: PASS. M8 is ready for a fresh independent L4 review. M8
  remains open; L4 has not started, and M9-M10 remain unstarted.

## L4 blocker remediation · six bounded findings · 2026-09-09

- Authorization: fix only the six reported M8 audit, durability, empty-store,
  re-audit, failure-classification, and evidence gaps. No M9-M10 or unrelated
  behavior was introduced.
- SQLite audit: now compares the actual tables, indexes, and constraint-bearing
  catalog SQL with an independently constructed schema from the exact ordered
  migrations. It also constructs every memory domain record and verifies
  lifecycle, bidirectional supersession, and same-owner relationship integrity.
- FAISS durability: successful generation publication now requires directory
  fsync after both final replacements. Directory-sync failure is classified as
  `unavailable_publication`; readiness is never returned from uncertain durable
  state.
- Empty degraded state: null-ID-only cleanup now creates or verifies the exact
  empty durable generation before a degraded service can be exposed. It still
  performs no guessed or targeted vector operation and reads no completion
  clock without an identity.
- Refreshed authority: every rebuild and every cleanup attempt is followed by a
  fresh SQLite inventory and a second exact durable-generation comparison.
  Successful and partial cleanup results therefore use refreshed pending,
  failed, and cleanup counts.
- Failure families: unsafe build inputs/candidate failures map to
  `unavailable_rebuild`; durable write/sync/replacement failures map to
  `unavailable_publication`; final reload/checksum/exact-set failures map to
  `unavailable_post_publication_verification`.
- Added evidence: exact schema/constraint alteration; invalid supersession
  relationship; directory-fsync success/failure; required empty generation;
  refreshed partial-cleanup audit; all three FAISS failure families; ambiguous
  pending supersession remaining pending, unlinked, and degraded; and
  deterministic shared process-lock serialization plus release after failure.
- Focused M8 result: `37 passed in 9.56s`.
- Locked M8 suite with the verified local tokenizer cache: `65 passed in
  7.04s`.
- Complete offline CPU-only suite: `427 passed in 19.35s` using the recorded
  immutable Hugging Face and tiktoken caches.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: all six reported blockers have maker remediation evidence. M8 remains
  open; no fresh independent L4 review has started, and M9-M10 remain unstarted.

## L4 blocker remediation · seven remaining findings · 2026-09-10

- Authorization: fix only the seven reported relationship-audit, FAISS failure-
  stage, composed-startup, partial-cleanup, real-adapter, cross-workflow-lock,
  and interrupted-supersession evidence gaps. No M9-M10 or unrelated behavior
  was introduced.
- Relationship authority: recovery inventory now rejects self-links, cycles,
  and every supersession edge whose replacement is not indexed, in addition to
  the existing owner, lifecycle, and bidirectional-link checks.
- Exact FAISS failure stages: recovery candidate construction and temporary-
  pair verification classify as `unavailable_rebuild`; final replacement and
  directory durability classify as `unavailable_publication`; durable final-
  pair verification and exact-set comparison classify as
  `unavailable_post_publication_verification`.
- Added evidence: a composed null-ID degraded startup performs safe current and
  historical service operations; true two-item cleanup records one completion,
  one later acknowledgement failure, and refreshed pending/vector counts; real
  FAISS build/publication/verification seams produce the three exact reason
  families; recovery serializes against admission, idempotent retry, and
  forgetting and releases the shared lock after failure; and a targeted
  supersession interrupted after durable vector publication remains pending,
  unlinked, degraded, and fail-closed.
- Focused recovery result: `49 passed in 11.30s`.
- Literal locked M8 suite with
  `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`:
  `77 passed in 11.35s`.
- Complete offline CPU-only suite: `439 passed in 25.45s`, using
  `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, and the recorded immutable Hugging Face and tiktoken
  caches.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: all seven remaining reported blockers have maker remediation evidence.
  M8 remains open; no fresh independent L4 review has started, and M9-M10
  remain unstarted.

## L4 blocker remediation · final relationship-audit finding · 2026-09-10

- Authorization: extend only the M8 recovery relationship audit so every
  persisted supersession edge must retain the M4 target invariants. No other
  recovery policy or M9-M10 behavior was added.
- Audit rule: in addition to the previously checked identity, owner, lifecycle,
  bidirectional, self-link, cycle, and indexed-replacement rules, every target
  must be indexed; every replacement must retain `explicit_user` authority;
  both normalized subjects must be present and equal; and both memory types
  must be equal.
- Direct SQLite-adapter regressions independently corrupt the target indexing
  state, replacement authority, normalized subject equality, and memory-type
  equality. Each audit returns `unavailable_authoritative_identity`.
- Focused M8 result: `53 passed in 15.95s`.
- Literal locked M8 suite with
  `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`:
  `81 passed in 11.78s`.
- Complete offline CPU-only suite: `443 passed in 27.59s`, using
  `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, and the recorded immutable Hugging Face and tiktoken
  caches.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: the final reported relationship-audit blocker has maker remediation
  evidence. M8 remains open; no fresh independent L4 review has started, and
  M9-M10 remain unstarted.

## L4 blocker remediation · supersession cardinality · 2026-09-10

- Authorization: fix only the remaining M8 relationship-audit cardinality
  finding. No other recovery policy or M9-M10 behavior was added.
- Audit rule: a persisted replacement may contain at most one `supersedes`
  target. Multiple distinct targets and duplicate entries for the same target
  both make recovery unavailable with `unavailable_authoritative_identity`.
- Direct SQLite-adapter regressions cover both corrupted shapes and verify the
  exact unavailable reason.
- Focused M8 result: `55 passed in 15.65s`.
- Literal locked M8 suite with
  `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`:
  `83 passed in 11.81s`.
- Complete offline CPU-only suite: `445 passed in 27.34s`, using
  `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, and the recorded immutable Hugging Face and tiktoken
  caches.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: the supersession-cardinality blocker has maker remediation evidence.
  M8 remains open; no fresh independent L4 review has started, and M9-M10
  remain unstarted.

## L4 blocker remediation · forgetting authority and real corruption · 2026-09-10

- Authorization: fix only the remaining M8 forgetting-identity, forgetting-
  timestamp, real-FAISS-corruption, and independent-SQLite-integrity findings.
  No other recovery policy or M9-M10 behavior was added.
- Global vector identity: every non-null forgetting vector ID, including a
  completed record, is checked against every other forgetting record and every
  live mapping. A known cleanup-pending record may retain the same ID in its own
  live mapping as required by M7; reuse by a different memory is unavailable
  with `unavailable_authoritative_identity`.
- Timestamp authority: every forgetting record must construct with aware UTC
  `requested_at` and the bound pending/complete `completed_at` shape; its
  memory tombstone must also be aware UTC and equal to `requested_at`.
  Violations return `unavailable_authoritative_identity`.
- Real FAISS evidence: incomplete, damaged, stale-model, wrong-dimension, and
  checksum-invalid durable pairs were created on disk and rebuilt from the
  audited SQLite inventory. The resulting generation reopens and contains the
  authoritative stable vector ID.
- SQLite integrity evidence: a catalog root-page corruption makes native
  `PRAGMA integrity_check` fail independently of a foreign-key violation and
  returns `unavailable_sqlite_integrity`.
- Focused M8 result: `67 passed in 11.99s`.
- Literal locked M8 suite with
  `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`:
  `95 passed in 12.27s`.
- Complete offline CPU-only suite: `457 passed in 27.88s`, using
  `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, empty
  `CUDA_VISIBLE_DEVICES`, and the recorded immutable Hugging Face and tiktoken
  caches.
- Ruff over `src` and `tests`: passed.
- Explicit-source mypy: passed with no issues in 29 source files.
- `git diff --check`: passed.
- State: the four remaining reported blockers have maker remediation evidence.
  M8 remains open; no fresh independent L4 review has started, and M9-M10
  remain unstarted.

## Fresh independent L4 review · APPROVE · 2026-09-10

- Verdict: `APPROVE`; no blocking findings.
- Independence: the reviewer inspected the complete tracked and untracked M8
  diff without editing files or rerunning tests.
- Boundary: every changed and untracked path conforms to the approved M8 freeze
  exceptions. No migration, dependency, completed-milestone test, M9, or M10
  file changed.
- Accepted authority evidence: global stable-vector identity covers live
  mappings and all non-null forgetting IDs, including completed records;
  completed cleanup requires no live mapping; and M7 request, deletion, and
  completion timestamps retain their aware-UTC and state invariants.
- Accepted durable-index evidence: real incomplete, damaged, stale-model,
  wrong-dimension, and checksum-invalid FAISS generations are reconciled and
  verified against authoritative stable IDs. Native SQLite integrity corruption
  is independently detected through `PRAGMA integrity_check`.
- Accepted remaining safety evidence: recovery uses the shared process lock,
  re-audits after rebuild and cleanup, re-verifies exact durable generations,
  durably publishes temporary pairs through final replacement and directory
  sync, and refuses service composition when readiness is unavailable.
- Accepted maker evidence: 67 focused tests, 95 locked M8 tests, 457 complete
  offline tests, Ruff, mypy, and diff-integrity checks passed.
- State: M8 remains open pending the canonical three-question human quiz and
  separate explicit closure authorization. M9-M10 remain unstarted.

## Canonical post-APPROVE human quiz · PASS · 2026-09-10

- Question 1 · authority choice: explained that SQLite is authoritative for
  ownership, lifecycle, deletion, mappings, and embeddings while FAISS is a
  derived search index; allowing FAISS to override SQLite could restore
  deleted, invalid, or wrong-owner data, whereas rebuilding from SQLite is safe
  and deterministic. Assessment: pass.
- Question 2 · missing stable identity: explained that an otherwise safe store
  with a pending memory lacking a trustworthy vector ID starts degraded and
  keeps that memory excluded; recovery must not invent an ID or vector, promote
  it to indexed, or prevent other safe memories from remaining usable.
  Assessment: pass.
- Question 3 · post-publication verification: explained that recovery must
  reload and verify the final durable FAISS index and metadata, including
  checksums, model, dimension, vector count, and the exact stable-ID set against
  audited SQLite; failure makes startup unavailable and prevents service
  exposure. Assessment: pass.
- Quiz result: `PASS 3/3`.
- State: independent L4 APPROVE and the canonical quiz are complete. M8 remains
  open pending explicit closure authorization; M9-M10 remain unstarted.

## Authorized milestone closure · 2026-09-10

- Authorization: the user explicitly authorized canonical M8 closure and
  prohibited source/test changes, staging, commits, pushes, or beginning M9-M10.
- Required human gates: fresh independent L4 verdict `APPROVE`; canonical quiz
  `PASS 3/3`.
- Completion scope: SQLite-authoritative recovery inventory; schema, integrity,
  identity, lifecycle, supersession, forgetting, timestamp, embedding, and
  stable-vector audits; exact rebuild of derived FAISS state from stored
  float32 embeddings; orphan removal; verified empty generations; atomic
  durable publication; degraded and unavailable readiness; M7 cleanup handoff;
  shared-lock startup orchestration; restart idempotency; and
  `demo-recovery`. Remote/distributed recovery, high availability,
  observability, fixed-workload evaluation, and M9-M10 behavior remain excluded.
- Closure verification: literal locked M8 suite `95 passed in 16.38s`;
  architecture suite `4 passed in 0.21s`; complete offline CPU-only suite `457
  passed in 27.91s`; Ruff passed; explicit-source mypy passed with 29 source
  files; `pip check` reported no broken requirements; CLI help exited 0 and
  listed all eight demos.
- Demo evidence: offline CPU-only `demo-recovery` exited 0 and reported a ready
  rebuilt generation, stable vector ID 1, one orphan removed, current memory
  selection, tombstone exclusion, and a fresh ready restart that reused the
  byte-identical durable files without rebuilding.
- Cache prerequisite: verification used
  `TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache` and
  the recorded offline CPU-only Hugging Face environment; no model or tokenizer
  download was attempted.
- Context graph: JSON parsed; 36 nodes, 43 edges, and eight invariants had valid
  endpoints and remained acyclic.
- `git diff --check`: exit 0.
- Closure state: M8 is complete. M9 is the next planned milestone but is
  explicitly unstarted pending separate G0 authorization; M10 is also unstarted.
