# M9 checkpoint

## G0 governance proposal · 2026-09-10

- State: `UNBUILT`
- BUILD: not activated
- Previous milestone: M8 closed after fresh independent L4 APPROVE, canonical
  human quiz PASS 3/3, and green closure verification.
- Objective: emit sufficient local structured decision and failure evidence for
  approved memory operations without exposing memory, query, authentication,
  secret, or sensitive-rejection content.
- Evidence: canonical Genesis records; M1-M8 operation/result contracts;
  `experiments/d4_engineering_reality/observability_analysis.md`;
  `design/threat_model.md`; `design/api_contracts.md`;
  `design/decision_records/ADR-007-privacy-isolation-and-forgetting.md`;
  `design/sprint_plan.md`; the context graph; current application ports,
  services, recovery, composition, CLI, and tests; Transparency and
  Observability wiki guidance; and Access Control wiki guidance.
- Current reality: the repository has structured application results and
  demonstration JSON but no unified runtime event contract, sink, deterministic
  event serialization, user pseudonymizer, or operation-wide privacy scan.
- Binding: `M9-privacy-safe-observability-bindings.md` defines the exact
  terminal/internal event names, field profiles, outcomes, reasons, HMAC rule,
  timing, ordering, serialization, non-interference, and privacy tests.
- Architecture: immutable event contracts and sink/clock interfaces are owned
  by the application layer. Local infrastructure serializes and delivers only
  allowlisted fields. Normal M9 composition injects all observability
  dependencies explicitly.
- Privacy: raw user identity is replaced by a domain-separated HMAC-SHA256
  pseudonym. Raw memory/query/authentication/secret content and free-form errors
  are forbidden.
- Failure rule: event-path failures never change primary workflow behavior and
  never trigger fallback or recursive logging. Invalid required observability
  configuration refuses observed service composition.
- Boundary: `M9-freeze-boundary-exceptions.md` authorizes only five M9
  governance paths. BUILD remains confined to the locked M9 source/test
  boundary unless a later exact exception is separately approved.
- Exact locked test command, future only:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py tests/regression -q`.
- Required demo, future only:
  `.venv/bin/python -m conversational_memory.entrypoints.cli
  demo-observability`.
- Future full verification: architecture tests; complete offline CPU-only
  suite; Ruff; explicit-source mypy; pip check; CLI help; the real
  `demo-observability`; context-graph validation; `git diff --check`; and
  `git status --short`.
- G0 verdict: `UNBUILT`. Every identified M9 policy choice is resolved, but M9
  remains unactivated pending explicit L1 BUILD authorization.
- Exclusions: production monitoring infrastructure, remote telemetry,
  compliance claims, fixed-workload evaluation, and all M10 behavior remain
  unstarted and unauthorized.

## Iteration 1 · L1 BUILD · strict event primitives · 2026-09-10

- Authorization: activate M9 and implement only the immutable event schema,
  typed event and timing ports, injected HMAC user pseudonymizer, deterministic
  privacy-safe JSON-lines serializer, capture sink, and focused unit evidence.
- Regression-first proof: the new focused test file initially exited 2 during
  collection with `ModuleNotFoundError` for
  `conversational_memory.application.events` before implementation existed.
- Contract: added all 13 approved event names, the closed outcome/reason/stage
  vocabularies, immutable strict field profiles, UTC timestamp and count
  invariants, exact event-specific required/allowed fields, and opaque
  forgetting `not_found` behavior.
- Privacy: added explicit domain-separated HMAC-SHA256 user pseudonymization,
  a closed forbidden-field set, free-text reason collapse, deterministic
  allowlist-only JSON-lines serialization, and a capture sink retaining only
  validated events and serialized lines.
- Ports: added application-owned `EventSinkPort` and separate
  `TelemetryClockPort`; neither is wired into an existing workflow.
- Iteration findings: the first implementation run exposed strict timestamp
  schema construction and boolean-as-literal-version defects; both were fixed
  within the event contract. Static checks then exposed only local export/style
  and frozen-set typing defects, which were corrected without widening scope.
- Focused result:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py -q` —
  `69 passed in 0.43s`.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 30 source files.
- State: iteration 1 complete. Service, recovery, startup, composition,
  infrastructure logging, CLI/demo, dependencies, migrations, completed-
  milestone tests, and M10 remain unchanged and unstarted.

## Iteration 2 · L1 BUILD · admission and retrieval instrumentation · 2026-09-10

- Authorization: instrument only `MemoryService` admission and retrieval with
  the approved terminal, storage, indexing, configuration, and admission-retry
  events; preserve all primary workflow semantics.
- Regression-first proof: the new integration suite initially reported
  `11 failed, 69 passed`; the service rejected the new `event_sink` injection
  because no observability constructor surface or workflow instrumentation yet
  existed. One nested temporary-store fixture also required its parent
  directory to be created before exercising the intended service failure.
- Admission: emits ordered idempotency lookup, embedding, pending persistence,
  vector publication, acknowledgement, and retry/replay evidence as applicable,
  followed by exactly one terminal event. Sensitive admission reasons collapse
  to `sensitive_admission_rejected` without changing the returned result.
- Retrieval: emits expiration/allowlist, query embedding, vector search, and
  hydration evidence as applicable, followed by exactly one terminal event
  containing exact candidate/returned counts, selected IDs, token budget, and
  used tokens. All four retrieval outcomes are covered distinctly.
- Exceptions: ordinary application exceptions retain their original type,
  reason, and control flow while receiving a final generalized terminal event;
  configuration mismatch emits its internal event before that terminal.
- Non-interference: sink, telemetry timing, pseudonymization, and serialization
  failures are contained at the event boundary and do not change results,
  exceptions, persistence, lifecycle-clock use, retries, or side effects.
- Privacy evidence: captured immutable objects and JSON lines contain no raw
  user ID, HMAC key, memory/query/value/credential content, forbidden key, or
  injected exception text.
- Focused result:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q` — `83 passed in 8.21s`.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 30 source files.
- State: iteration 2 complete. Forgetting, recovery, startup, composition,
  infrastructure logging, CLI/demo, dependencies, migrations, completed-
  milestone tests, and M10 remain unchanged and unstarted.

## Iteration 3 · L1 BUILD · forgetting instrumentation · 2026-09-10

- Authorization: instrument only `MemoryService.forget` with the approved
  lookup, initiation, vector-removal, acknowledgement, and terminal events while
  preserving every M7 state transition and failure result.
- Regression-first proof: after adding the new forgetting regressions, the
  focused suite reported `3 failed, 87 passed`; successful, opaque-not-found,
  and cleanup-pending workflows produced no forgetting events before the
  implementation was wired.
- Ordering: successful first-time forgetting emits owner-scoped lookup,
  tombstone initiation, vector removal, and cleanup acknowledgement events in
  operation order, then exactly one `forgetting_completed` terminal event.
- Opacity: lookup events omit the caller-supplied target ID. Nonexistent and
  cross-owner requests produce the same `not_found` result and event shape,
  with no memory ID, lifecycle-clock read, or mutation.
- Retry/state evidence: completed replay performs only lookup before the
  `already_forgotten` terminal; removal failure returns
  `physical_cleanup_pending`; acknowledgement failure remains pending; and a
  later exact retry completes existing cleanup without repeating initiation.
- Non-interference: sink, timing, pseudonymization, and serialization failures
  were exercised through real successful tombstoning and targeted FAISS cleanup
  and did not alter the result, lifecycle-clock count, durable forgetting state,
  or physical-cleanup behavior.
- Privacy: captured objects and JSON lines retain only the approved authoritative
  memory ID after owner resolution and contain no raw user, key, memory content,
  query/value/credential data, removal/acknowledgement error text, or forbidden
  fields.
- Focused result:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q` — `91 passed in 9.45s`.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 30 source files.
- State: iteration 3 complete. Recovery, startup, composition, infrastructure
  logging, CLI/demo, dependencies, migrations, completed-milestone tests, and
  M10 remain unchanged and unstarted.

## Iteration 4 · L1 BUILD · direct recovery instrumentation · 2026-09-10

- Authorization: instrument only direct `RecoveryCoordinator.recover` calls
  with the approved inventory, generation-reconciliation, forgetting-vector
  adoption/acknowledgement, and terminal readiness evidence while preserving
  every M8 recovery decision and state transition.
- Regression-first proof: after adding the direct-recovery observability
  regressions, the integration suite reported `14 failed, 22 passed`; every new
  case failed because `RecoveryCoordinator` did not yet accept the explicit
  observability dependencies.
- Ordering: each attempted authoritative inventory, generation reconciliation,
  forgetting-vector adoption, and cleanup acknowledgement emits exactly one
  synchronous completed or failed internal event before one
  `recovery_completed` terminal event. Rebuild re-audit/reverification therefore
  remains visible in its actual operation order.
- Exact readiness evidence: the terminal event records the returned M8
  readiness/reason together with configured model/dimension, verified vector
  count, rebuild flag, removed-orphan count, and refreshed pending, failed, and
  cleanup-pending counts. Ready rebuild/reuse, degraded state, and all seven
  approved unavailable reason families are covered.
- Privacy and non-interference: direct recovery emits no user reference and
  never pseudonymizes audited owner identities. Captured objects/JSON lines
  exclude raw owner identity, HMAC key, forbidden fields, and storage/index
  errors. Sink, telemetry timing, pseudonymizer, and serialization failures do
  not change readiness, reason, locking, inventory, reconciliation, or cleanup.
- Focused result:
  `.venv/bin/python -m pytest tests/integration/test_recovery.py
  tests/unit/application/test_events.py
  tests/integration/test_observability.py -q` — `127 passed in 11.13s`.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 30 source files.
- Diff integrity: `git diff --check` — passed.
- State: iteration 4 complete. Startup composition, infrastructure delivery,
  CLI/demo, dependencies, migrations, completed-milestone tests, and M10 remain
  unchanged and unstarted.

## Iteration 5 · L1 BUILD · local composition and startup · 2026-09-10

- Authorization: add only the bounded local JSON-lines sink and system telemetry
  clock, explicitly inject the sink/telemetry clock/HMAC key at composition, and
  emit recovery-before-startup readiness evidence without changing M8 startup
  decisions.
- Regression-first proof: the new startup/infrastructure regressions initially
  exited 2 during collection because `JsonLineEventSink` did not yet exist or
  export from infrastructure.
- Infrastructure: `JsonLineEventSink` serializes one already-validated event at
  a time to an injected binary stream under a local lock and flushes it; it does
  not select a path, persist an audit store, retry, or deliver remotely.
  `SystemTelemetryClock` supplies independent aware-UTC wall time and monotonic
  nanoseconds without reading the lifecycle clock.
- Explicit configuration: composition accepts the sink, telemetry clock, and
  raw HMAC key only as one explicit triplet and constructs the application-owned
  pseudonymizer. Omitting all three preserves the existing unobserved service
  and recovered-startup paths. Partial or invalid configuration raises
  `ConfigurationError("invalid_observability_configuration")`; when usable
  delivery/timing components exist it emits `configuration_failed` followed by
  one `startup_unavailable` and exposes no service.
- Startup ordering: observed startup emits the direct recovery internal and
  `recovery_completed` events first, then exactly one of `startup_ready`,
  `startup_degraded`, or `startup_unavailable` with the same exact readiness,
  reason, model/dimension, vector/rebuild/orphan, and unresolved-count evidence.
- Coverage: real temporary SQLite/FAISS composition proves rebuilt ready,
  existing-generation ready, degraded, unavailable, and invalid-observability
  startup. It also proves service injection, pseudonymized later operations,
  forbidden-field/privacy scans, one startup terminal, and sink/timing/
  serialization non-interference. Existing unobserved startup tests remain
  green.
- Focused result:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py tests/integration/test_recovery.py
  tests/integration/test_startup_readiness.py -q` —
  `142 passed in 11.45s`.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 31 source files.
- Diff integrity: `git diff --check` — passed.
- State: iteration 5 complete. CLI/demo, remote or persistent delivery,
  dependencies, migrations, completed-milestone tests, and M10 remain unchanged
  and unstarted.

## Iteration 6 · L1 BUILD · `demo-observability` · 2026-09-10

- Authorization: add only the bounded `demo-observability` parser/dispatch and
  real temporary local observed-composition demonstration, using an explicit
  demo-only HMAC key and the existing capture/canonical-JSON-lines sink.
- Regression-first proof: the new command regression initially failed because
  `demo-observability` was not in the CLI parser's closed command set.
- Demonstration: one temporary durable SQLite/FAISS store exercises rebuilt
  ready startup, accepted admission, collapsed sensitive rejection, selected
  retrieval, completed owner forgetting, degraded startup with excluded pending
  work, and authoritative-identity unavailable startup that refuses service
  exposure. All operations use the existing application, recovery, composition,
  and real local adapter paths.
- Determinism: the demo injects stable opaque memory IDs, one explicit demo-only
  HMAC key, a fixed lifecycle clock, and a deterministic telemetry clock. Stdout
  is exclusively the capture sink's canonical JSON lines in synchronous event
  order; no result payload, prose, memory/query/authentication/secret content,
  or exception text is printed.
- Privacy/order evidence: the command verifies every required operation and
  readiness terminal is present, exactly three startup terminals are emitted,
  each startup terminal immediately follows `recovery_completed`, and no
  forbidden key or private user/content/query/credential/auth/HMAC sentinel is
  present. The integration regression parses every output line, enforces the
  closed field allowlist, event counts/order, and repeats the recursive privacy
  scan.
- Focused result:
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q` —
  `115 passed in 9.47s`.
- Real offline demo:
  `env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m conversational_memory.entrypoints.cli
  demo-observability` — exit 0; emitted only validated JSONL on stdout and proved
  accepted/sensitive/retrieval/forgetting plus ready/degraded/unavailable-safe
  recovery/startup evidence.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — passed with no issues
  in 31 source files.
- Diff integrity: `git diff --check` — passed.
- State: iteration 6 complete. Remote or durable audit delivery, dependencies,
  migrations, completed-milestone tests, and M10 remain unchanged and unstarted.

## Maker verification · 2026-09-10

- Authorization: run M9 maker verification only, stop at the first blocker, and
  update only this checkpoint and `CURRENT.md` if every gate passes. No source,
  test, dependency, staging, commit, push, L4, closure, or M10 work was
  authorized or performed.
- Locked M9 suite:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py tests/regression -q` —
  `127 passed in 12.31s`, exit 0.
- Architecture suite:
  `.venv/bin/python -m pytest tests/architecture -q` —
  `4 passed in 0.29s`, exit 0.
- Complete offline CPU-only suite:
  `env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests -q` — `572 passed in 33.41s`, exit 0.
- Real offline `demo-observability`: the same offline CPU/model/tokenizer-cache
  environment with `.venv/bin/python -m conversational_memory.entrypoints.cli
  demo-observability` exited 0. The validated JSON-lines stream demonstrated
  ordered recovery/startup evidence for ready, degraded, and unavailable states;
  accepted admission; collapsed sensitive rejection; selected retrieval; and
  completed forgetting. Model-loading progress was emitted on stderr, not in
  the structured stdout event stream.
- Ruff: `.venv/bin/python -m ruff check src tests` — `All checks passed!`,
  exit 0.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — no issues in 31
  source files, exit 0.
- Dependency consistency: `.venv/bin/python -m pip check` — no broken
  requirements, exit 0. Pip reported only that its unwritable default cache was
  disabled.
- CLI help:
  `.venv/bin/python -m conversational_memory.entrypoints.cli --help` — exit 0
  and listed all nine demos including `demo-observability`.
- Context graph validation: repository JSON parsed; all node identifiers were
  unique; all edge endpoints were present; 36 nodes, 43 edges, and eight
  invariants were reported; and the graph was acyclic, exit 0.
- Diff integrity: `git diff --check` — exit 0 before and after the mandatory
  checkpoint-only evidence update.
- Maker verdict: PASS. M9 is ready for a fresh independent L4 review, which has
  not begun. M9 remains open; remote or durable audit delivery and M10 remain
  explicitly excluded and unstarted.

## L2 DEBUG · six supplied L4 blockers · 2026-09-17

- Authorization: fix only the six M9 L4 findings covering terminal failure
  events, ordinary-exception observation and event isolation, complete startup
  boundaries, unresolved caller supersession identifiers, serializer
  revalidation, and missing mandatory regression evidence. The earlier maker
  PASS remains historical evidence, not an independent approval.
- G1: skills considered and used: agentic-swe-master, modular-architecture,
  production-readiness. Canonical loops, adapter, binding, freeze boundary,
  CURRENT, relevant implementation, tests, and wiki guidance were inspected.
  The optional detective/systematic-debugging skill is not in the available
  catalog; diagnosis used the supplied findings and direct regression evidence.
- Cause: wrappers observed only ApplicationError; retrieval validation could
  select an admission-only reason; failed forgetting profiles required an ID
  that lookup had not resolved and rejected configuration reasons; conversion
  occurred outside delivery guards; local constructors preceded the startup
  guard; supersession lookup supplied a caller ID to events; and serialization
  trusted an existing frozen instance without complete strict revalidation.
- Regression-first result: the initial `-k l4` integration run reported
  `13 failed, 46 deselected in 7.76s`, exit 1. Eight failures demonstrated
  missing terminal/profile evidence or serializer validation bypass. Five
  failures were erroneous test-fixture calls to a nonexistent repository
  `close()` method; those calls were removed without weakening assertions.
- Correction: admission, retrieval, and forgetting observe ordinary Exception
  failures and use bare re-raise. Operation-specific closed reasons and valid
  failed-forgetting profiles retain opacity when no ID is resolved. Complete
  terminal conversion is isolated, including unknown recovery reasons, while
  BaseException propagation remains unchanged. Configuration events precede
  applicable terminals without duplicate emission or lifecycle-clock reads.
- Startup: local construction is observed before SQLite, model, tokenizer,
  recovery-adapter, final-index, and service construction. Recovered startup
  uses the same boundary; readiness counts already obtained from recovery are
  retained on later failure. Exactly one unavailable startup terminal is
  offered on failure, with configuration_failed when applicable. The direct
  dependency-wiring helper observes construction failures without inventing an
  audited successful-readiness result. Existing SQLite StorageError translation
  is preserved; all other original constructor exceptions are re-raised.
- Privacy: supersession lookup emits no unresolved caller target ID. Every
  serialization reconstructs MemoryEvent through the strict contract before
  producing allowlisted JSONL. Recursive scans cover captured dataclass
  objects, decoded JSON, and exact bytes; forbidden/unknown keys and injected
  user, content, query, subject/value, credential/authentication, HMAC, and
  exception sentinels are checked.
- G2: added 63 tests covering ordinary failures and identity, process-control
  propagation, pending/failed retries, historical retrieval, concurrent request
  correlation, validation/tokenizer/lifecycle-clock/lookup failures, startup
  constructors and reason families, event conversion/validation failures,
  unknown recovery reasons, and serializer bypass rejection.
- Focused command:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q` —
  `178 passed in 13.69s`, exit 0.
- Architecture: `.venv/bin/python -m pytest tests/architecture -q` —
  `4 passed in 0.26s`, exit 0.
- Full offline CPU-only suite:
  `env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests -q` —
  `635 passed in 34.71s`, exit 0.
- Real demo: the identical recorded offline CPU/cache environment with
  `.venv/bin/python -m conversational_memory.entrypoints.cli
  demo-observability` exited 0. Ordered ready/degraded/unavailable
  recovery/startup, accepted admission, collapsed sensitive rejection, selected
  retrieval (16/128 tokens), and completed forgetting were emitted as validated
  JSONL. Model-loading progress remained on stderr.
- Ruff: `.venv/bin/python -m ruff check src tests` — passed, exit 0.
- Explicit-source mypy:
  `.venv/bin/python -m mypy src/conversational_memory` — no issues in 31
  source files, exit 0.
- `git diff --check` — exit 0; checked again after checkpoint evidence edits.
- G3: no exact runtime token meter is exposed; no budget or milestone closure
  is inferred from verification. G4: requested checks pass. G5: fresh
  independent review is not started or approved by this maker correction.
- Scope: changed only events/service/recovery/composition, the two authorized
  M9 test files, and M9/CURRENT evidence. Existing work remains intact; no
  primary memory policy, migrations, dependencies, completed-milestone tests,
  staging, commit, push, closure, or M10 behavior was changed or started.

## L2 DEBUG — three remaining L4 blockers (2026-09-17)

- G1: the requested correction is restricted to trusted request correlation,
  direct/composed recovery configuration ordering, and admission failure-state
  telemetry. Earlier evidence and limitations remain preserved; M9 stays open.
- Regression-first command:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/integration/test_observability.py -q
  -k 'm9_trusted_correlation or m9_mark_failed or m9_recovery_configuration'`
  — `13 failed, 105 deselected in 10.24s`, exit 1 before implementation fixes.
- Trusted IDs: event validation now matches the existing non-whitespace-content
  request contract without trimming. Admission, retrieval, and forgetting retain
  the exact ID on ordered internal events and their single terminal event.
- Recovery: configuration mismatch offers isolated `configuration_failed`
  before `recovery_completed`. Startup tracks recovery ownership per invocation
  and does not offer the configuration event again, including delivery failure.
  Original exceptions, readiness, and service refusal are unchanged.
- Admission: the existing `mark_failed` write runs through storage observation,
  emitting completed/failed before the admission terminal. Successful writes
  still leave FAILED; failed writes still leave PENDING. Exception text is not
  included in events. The binding/manifest document the closed stage addition
  and exact correlation/configuration ordering.
- G2: added 20 regressions for these cases, strict event serialization, exact
  whitespace correlation, real SQLite configuration mismatch, original exception
  identity, isolated delivery, state preservation, ordering, and privacy scans.
- Focused:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q`
  — `198 passed in 15.00s`, exit 0.
- Architecture: `.venv/bin/python -m pytest tests/architecture -q`
  — `4 passed in 0.24s`, exit 0.
- Full offline CPU-only command:
  `env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests -q`
  — `655 passed in 36.57s`, exit 0.
- Real demo: the same recorded offline CPU/cache environment with
  `.venv/bin/python -m conversational_memory.entrypoints.cli demo-observability`
  — exit 0. Validated JSONL demonstrated ordered ready/degraded/unavailable
  recovery/startup, accepted admission, sensitive rejection, selected retrieval
  (16/128 tokens), and completed forgetting. Weight progress was on stderr.
- `.venv/bin/python -m ruff check src tests` — all checks passed, exit 0.
- `.venv/bin/python -m mypy src/conversational_memory` — no issues in 31
  source files, exit 0.
- `git diff --check` — exit 0, repeated after evidence updates.
- G3/G4: no exact runtime token meter is exposed; all requested checks pass.
  G5: fresh independent L4 is neither started nor approved by this correction.
- Scope: only events/service/recovery/composition, authorized M9 tests,
  binding/manifest, and mandatory M9/CURRENT evidence changed in this iteration.
  Existing work, memory policies, dependencies, migrations, and historical
  limitations remain intact. No staging, commit, push, closure, or M10 work.

## L2 DEBUG — two remaining L4 blockers (2026-09-17)

- G1: skills considered/chosen: agentic-swe-master (lifecycle routing),
  modular-architecture (composition/application boundaries), production-readiness
  (fail-fast configuration and isolated observability). Relevant Dependency-Rule,
  Transparency-and-Observability, and Ruthless-Testing wiki pages were read from
  `/mnt/c/Users/Asus/Desktop/agentic-swe-kit/wiki` read-only.
- Regression-first:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/integration/test_observability.py -q
  -k 'noncallable_sink or cleanup_configuration_degraded'`
  — `11 failed, 122 deselected in 10.03s`, exit 1 before the fixes.
- Sink validation now requires a callable `emit` before startup work, rather
  than relying on runtime protocol attribute presence. `emit = 1` raises exactly
  `ConfigurationError("invalid_observability_configuration")` across local,
  recovered, and dependency-wired composition, with no startup/clock/embed work.
- Recovery catches cleanup `ConfigurationError` separately and offers an
  isolated `configuration_failed` at that catch site before its degraded
  recovery terminal. Existing cleanup_pending state, mappings, tombstone,
  re-audit, readiness reason/counts, and usable degraded composition are unchanged.
  Composed startup adds only its startup_degraded terminal, not another
  configuration event. Sink rejection does not cause another delivery attempt
  or change recovery behavior.
- G2: added 11 regressions covering all three composition paths, direct/composed
  cleanup clock and acknowledgement failures, one configuration-event attempt,
  ordered events, exactly one recovery terminal, preserved authoritative state,
  recursive object/JSON/byte privacy scans, and rejecting-sink isolation.
- Focused:
  `env TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py -q`
  — `209 passed in 18.26s`, exit 0.
- Architecture: `.venv/bin/python -m pytest tests/architecture -q`
  — `4 passed in 0.18s`, exit 0.
- Full offline CPU-only:
  `env HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache
  .venv/bin/python -m pytest tests -q`
  — `666 passed in 36.11s`, exit 0.
- Real demo: the identical offline CPU/cache environment with
  `.venv/bin/python -m conversational_memory.entrypoints.cli demo-observability`
  — exit 0; structured ready/degraded/unavailable recovery/startup and admission,
  sensitive rejection, retrieval, and forgetting output completed. Model-loading
  progress remained on stderr.
- `.venv/bin/python -m ruff check src tests` — final all checks passed, exit 0.
  Initial lint found C408 in the new test options dictionary; replaced that
  constructor with a literal and reran successfully.
- `.venv/bin/python -m mypy src/conversational_memory` — no issues in 31
  source files, exit 0.
- `git diff --check` — exit 0, repeated after mandatory evidence updates.
- G3: no exact runtime token meter is available. G4: requested checks pass.
  G5: fresh independent L4 remains pending; no review or closure is inferred.
- Task-only files: application/recovery.py, composition/__init__.py,
  tests/integration/test_observability.py, and M9/CURRENT checkpoint evidence.
  Binding/manifest and all pre-existing work are preserved. Historical review
  limitations remain unchanged. No dependencies, migrations, staging, commit,
  push, closure, or M10 work.

## Fresh independent M9 L4 review — APPROVE (recorded 2026-09-17)

- Source: the user's supplied fresh independent M9 L4 report. Verdict:
  **APPROVE**, no remaining blockers. The reviewer edited no files and reran
  no tests. This entry records that external verdict; it is not a new maker
  review or execution. Earlier rejections, corrections, incidents, and
  limitations are preserved.
- Event ownership/validity: application-owned enums define approved names,
  outcomes, reasons, and stages. Frozen strict MemoryEvent profiles enforce
  required/allowlisted fields, types, UTC timestamps, counts, correlation pairs,
  retrieval invariants, and forgetting opacity
  (`src/conversational_memory/application/events.py:363`).
- Privacy/pseudonymization: allowlisted decision metadata only;
  domain-separated HMAC-SHA256 user references with an explicit key of at least
  32 bytes; sensitive rejection collapses to the approved general code;
  free-form errors are excluded (`application/events.py:475`, `:503`).
- Admission/replay/retry: attempted lookup, embedding, persistence,
  publication, and acknowledgement stages are observed; exact stored matches
  emit retry/replay evidence; wrappers offer one terminal for results and
  ordinary exceptions (`application/service.py:146`, `:614`).
- Retrieval: current and historical owner-scoped allowlist/search/hydration
  behavior remains intact. Terminals record four outcomes, exact candidate and
  returned counts, selected IDs, budget, and tokens without query or memory
  content (`application/service.py:279`, `:896`).
- Forgetting: lookup, tombstone initiation, removal, acknowledgement,
  cleanup-pending, and completed replay remain unchanged. Nonexistent and
  cross-owner requests are equally opaque; unresolved caller IDs are omitted
  (`application/service.py:481`).
- Recovery/startup: inventory, reconciliation, adoption, and acknowledgement
  precede recovery completion; recovery precedes startup readiness; recovered
  counts survive later construction failure; unavailable service exposure is
  refused. Configuration evidence is ordered without duplicate delivery attempts
  (`application/recovery.py:191`, `composition/__init__.py:304`).
- Configuration: the explicit sink/telemetry-clock/HMAC-key triplet is resolved
  before startup; callable methods are validated; partial/invalid observed
  configuration raises the required ConfigurationError
  (`composition/__init__.py:359`).
- Timing/ordering: independent monotonic durations and completion-time UTC;
  invalid clock values suppress affected emissions safely; synchronous internal
  events precede terminals; concurrent correlation is covered
  (`application/service.py:992`).
- Serialization/delivery: every field is reconstructed and strictly validated
  before deterministic UTF-8 JSONL. The injected-stream sink serializes, locks
  delivery, checks short writes, and flushes (`application/events.py:521`,
  `src/conversational_memory/infrastructure/observability.py:15`).
- Non-interference: ordinary pseudonymization, timing, conversion, validation,
  serialization, and sink failures stay within observability guards. No repeated
  primary work, changed results/exceptions, extra lifecycle-clock reads,
  fallback, or recursive logging; process-control exceptions propagate
  (`application/events.py:29`). All shortened application/composition references
  above are relative to `src/conversational_memory/`.
- Latest blocker resolution: callable `emit` is required before startup.
  Local/recovered/dependency-wired regressions prove the exact exception and no
  startup work (`tests/integration/test_observability.py:64`). Caught cleanup
  ConfigurationError emits isolated configuration_failed before degraded
  completion, preserving state/readiness/counts and usable degraded composition.
  Clock/acknowledgement, direct/composed, rejecting-sink, ordering, privacy, and
  single-attempt coverage were inspected (`application/recovery.py:310`,
  `tests/integration/test_observability.py:95`).
- Earlier fixes remain resolved: ordinary-exception terminals, complete
  conversion isolation, startup constructor boundaries, unresolved
  supersession-ID privacy, serializer revalidation, exact trusted request IDs,
  mark_failed coverage, and recovery configuration ordering. The report points
  to the earlier correction evidence in this checkpoint (reported line 339).
- Coverage/verification: inspected suites cover required operation outcomes,
  failure seams, ordering, correlation, recursive object/JSON/byte privacy
  scans, and non-interference. The reviewer accepted existing maker execution
  records: **209 focused, 4 architecture, 666 full offline tests passed**, real
  demo-observability, Ruff, explicit-source mypy, and diff integrity. These
  executions were not independently rerun by this reviewer (latest evidence
  cited at reported checkpoint line 475).
- Boundary: complete changed/untracked paths fit locked M9 source/test scope
  and the five governance exceptions
  (`.genesis/decisions/M9-freeze-boundary-exceptions.md:7`). No
  completed-milestone/architecture test, dependency, migration, PLAN, DONE,
  context-graph, or implementation-notes change is part of M9's implementation
  diff.
- Scope/exclusions: local observability only; no remote telemetry, monitoring
  backend, durable audit database, compliance claim, workload evaluation, or
  M10 behavior. Real demo uses local composition and captured structured
  evidence (`src/conversational_memory/entrypoints/cli.py:1360`,
  `.genesis/decisions/M9-privacy-safe-observability-bindings.md:364`).
- G5: independent APPROVE recorded. Human quiz is still pending; M9 is open
  and requires explicit closure authorization after the quiz. M10 is unstarted.
- Recording-only task: agentic-swe-master guided lifecycle state recording;
  only M9.md and CURRENT.md changed. `git diff --check` passed after updates.
  No verification reruns, source/test edits, staging, commits, pushes, or closure.

## Canonical post-APPROVE human quiz — PASS 3/3 (recorded 2026-09-17)

Questions were asked one at a time after independent L4 APPROVE. The human
provided each answer before assessment; no answers were supplied in advance.
The concise answers below summarize the actual human responses.

1. **Design decision:** Why use a strict field allowlist and an injected HMAC
   user reference instead of logging full requests and scrubbing afterward?
   **Human answer:** Only approved operational fields can enter events; broad
   request logging risks leaking new or missed content, queries, credentials,
   or errors. HMAC provides stable correlation without exposing raw identity;
   unlike a plain hash of a low-entropy ID, it requires the secret key.
   **Assessment: PASS.** Correct allowlist boundary and keyed pseudonymization
   rationale, without claiming anonymity or permission to log private payloads.
2. **Edge case:** What happens if the sink raises an ordinary delivery
   exception? Should M9 retry primary work or log the sink error another way?
   **Human answer:** Preserve the same memory result or original exception.
   Contain the sink failure without repeating primary work or fallback logging,
   which could duplicate work, change behavior, or leak details. M9 has no
   durable-delivery guarantee.
   **Assessment: PASS.** Correct non-interference, no recursive/fallback
   logging, and no invented delivery guarantee.
3. **Change impact:** What does M9 add to recovery/startup, and what remains
   unchanged about readiness and service exposure?
   **Human answer:** Structured internal recovery stages, a recovery terminal,
   and one ready/degraded/unavailable startup event. Preserve M8 readiness:
   ready/degraded service remains usable; unavailable startup refuses exposure.
   Observability failures cannot change that behavior.
   **Assessment: PASS.** Correct evidence-only impact and unchanged readiness
   and service-exposure policy.

- Outcome: **PASS 3/3**. Independent L4 APPROVE remains recorded; earlier
  evidence, incidents, and limitations are preserved.
- M9 is eligible for explicit closure authorization but is **not closed**.
  M10 remains explicitly unstarted.
- Task scope: only M9.md and CURRENT.md updated. `git diff --check` passed.
  No verification reruns, source/test edits, staging, commits, or pushes.

## Authorized formal M9 closure — 2026-09-17

- Authorization: the user explicitly requested formal M9 closure, limited
  changes to these six records, and prohibited M10 implementation, staging,
  commits, and pushes. Required gates are recorded above: fresh independent
  L4 **APPROVE**, canonical human quiz **PASS 3/3** with actual Q&A.
- G1: agentic-swe-master used for canonical lifecycle closure; no new design
  or implementation work. G2: six canonical records synchronized. No exact
  runtime token meter is exposed; no token-usage claim is inferred.
- Completion scope: immutable strict application-owned events; closed typed
  fields/names/outcomes/reasons/stages; explicit HMAC user pseudonymization;
  correlated service admission/replay/retry/retrieval/forgetting evidence;
  ordered direct recovery and startup readiness/configuration events;
  independent timing; deterministic revalidated JSONL; local injected-stream
  and capture sinks; recursive privacy scans; ordinary telemetry-failure
  non-interference; and real demo-observability. Memory behavior and M8 readiness
  remain unchanged. No remote telemetry, monitoring backend, durable audit
  database/delivery guarantee, compliance claim, or M10 evaluation is included.
- All model-dependent closure commands used this task-local environment:
  `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 CUDA_VISIBLE_DEVICES=
  CONVERSATIONAL_MEMORY_MODEL_CACHE=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface/hub
  HF_HOME=/home/shubham/.cache/conversational-memory-intelligence-m1/huggingface
  TIKTOKEN_CACHE_DIR=/mnt/c/Users/Asus/AppData/Local/Temp/data-gym-cache`.
  Commands below use `.venv/bin/python` in
  `/home/shubham/projects/conversational-memory-intelligence`; no downloads,
  dependency changes, or persistent environment settings were made.
- Literal locked suite (with `env` plus the recorded environment):
  `.venv/bin/python -m pytest tests/unit/application/test_events.py
  tests/integration/test_observability.py tests/regression -q`
  — `221 passed in 21.82s`, exit 0.
- Architecture: `.venv/bin/python -m pytest tests/architecture -q`
  — `4 passed in 0.28s`, exit 0.
- Full offline suite (same `env`):
  `.venv/bin/python -m pytest tests -q`
  — `666 passed in 38.37s`, exit 0; no skips reported.
- Real demo (same `env`): `.venv/bin/python -m
  conversational_memory.entrypoints.cli demo-observability` — exit 0.
  Validated JSONL showed ready recovery before startup_ready, accepted admission,
  general sensitive rejection, selected retrieval with 16/128 tokens, completed
  forgetting, degraded recovery before startup_degraded, and unavailable
  recovery before startup_unavailable. Immutable CPU-only model loading used
  local caches; weight-loading progress remained on stderr.
- `.venv/bin/python -m ruff check src tests` — all checks passed, exit 0.
- `.venv/bin/python -m mypy src/conversational_memory` — no issues in 31
  source files, exit 0.
- `.venv/bin/python -m pip check` — no broken requirements found, exit 0.
  Non-failing warning: `/home/shubham/.cache/pip` or its parent was not owned
  or writable for this process, so pip disabled its cache. No repairs attempted.
- `.venv/bin/python -m conversational_memory.entrypoints.cli --help`
  — exit 0; all nine demo commands listed, including demo-observability.
- Context graph: generated metadata refreshed and M9 privacy/non-interference
  invariant added; existing 36-node/43-edge topology preserved. Closure
  validation parses JSON, checks unique nodes/edges, valid endpoints,
  acyclicity, and all nine non-empty invariants.
- `git diff --check` — exit 0 after closure updates.
- G4/G5: closure checks pass; existing fresh independent APPROVE retained.
  Earlier maker/reviewer execution distinctions, incidents, review-skill
  limitations, cache provenance/location, and isolation history remain preserved
  in the linked checkpoint records. This closure does not resolve or erase them.
- Closed state: **M9 complete**. **M10 next, explicitly unstarted**, pending
  separate G0 authorization; no preflight, BUILD, or workload evaluation begun.
- Exact task-only files: `.genesis/checkpoints/M9.md`,
  `.genesis/checkpoints/CURRENT.md`, `.genesis/PLAN.md`, `.genesis/DONE.html`,
  `.genesis/implementation-notes.html`, `.genesis/context-graph.json`.
  No source/tests, migrations, dependencies, staging, commits, or pushes.
